# SPLIT — 1차(디자인) / 2차(기능) 재분류

작성일 2026-07-29 · 대상 `SPEC-fd-ver3.md` §8 사용자 결정 대기 목록 23건
worktree `/Users/hyunsoo/uwellnow/winningpage-free-diagnosis` · 브랜치 `free-diagnosis-ver2` (HEAD `7f8e595`)

**본 문서는 SPEC-fd-ver3.md §8 을 대체한다.** 수치·좌표 원값은 여전히 개별 화면 md 소관.

---

## 0. 재분류 전제 (신규 지시)

| # | 전제 | 파급 |
|---|---|---|
| A | **헤더/푸터 이 브랜치 범위에서 완전 제외.** 현행 `Header.jsx`(`fixed` + `h-[84px]` + `max-w-[1500px]`) / `SiteFooter.jsx` 그대로. 64px 헤더·신규 푸터·로고 SVG 는 `landing-renewal` 소관 | 전역정책 3.1-4(헤더 64px, `pt-16`) **무효** → 본문 오프셋은 `pt-[5.25rem]`(84px). 에셋 재추출 6건 → **4건**. Phase 0-2/0-3(브랜치 선행 머지) **불필요** |
| B | **1차 = 디자인 구현.** 마크업·스타일·정적 렌더링·상태 전환(선택/hover/focus) | DB·저장·채점·PDF 전부 1차에서 빠짐 |
| C | **2차 = 기능 구현.** 스키마 확장, 응답 저장, 채점 로직, PDF, 입시 마스터, 관리자 연동 | SPEC Phase 3·5·6-2/6-3 전부 2차 |

판정 4분류: **(가) 확정됨** / **(나) 1차 블로커** / **(다) 2차로 이관** / **(라) 헤더푸터 제외로 무효화**

---

## 1. 23건 판정표

| # | 항목 | 판정 | 근거 (1줄) |
|---|---|---|---|
| 1 | 구세대 프레임 `10355`/`13222` 처리 | **(가)** | 확정 전제 4 — selected 레퍼런스로만 활용, 구현 대상 제외 |
| 2 | 12번 문항 확정 (19문항 산술) | **(가)** | 확정 전제 2 — 1889:9866 두 번째 Q10(14지 max3), 뱃지 "10"은 오기 |
| 3 | Q13/14/16/17/18 hug 칩 전환 방식 | **(가)** | 권장 (b) `flex-wrap` 이 전역 반응형 원칙에서 자동 도출, 행 하드코딩은 이미 배제됨 |
| 4 | Q19 멀티라인 전환 방식 | **(가)** | KISS·사용처 1곳 → `ConditionalTextInput` 에 `multiline` prop. 사용자 결정 불요(구현 세부) |
| 5 | 설문 페이지 분할 구조 | **(가)** | 확정 전제 1 — 5스텝 3/5/2/4/5, 라우팅 `/free-diagnosis/survey/:step` |
| 6 | 최종 CTA 활성 상태 디자인 | **(나)** | 제출 버튼 활성색은 1차 렌더 대상인데 시안에 비활성 `#D7D7D7` 만 존재 → **D4** |
| 7 | 응답 저장 도입 여부 | **(다)** | 전제 C + 확정 전제 5 명시 이관. 1차는 클라이언트 state 만 |
| 8 | 채점 규칙 확정 | **(다)** | 도메인 로직, 전제 C. 1차 리포트는 시안 고정값(더미)으로 렌더 |
| 9 | 서술 3문단·강점/보완 불릿 생성 방식 | **(다)** | 생성 로직 = 기능. 1차는 시안 원문 하드코딩 |
| 10a | **selected** 상태 정본 | **(가)** | 확정 전제 3 — bg `#E9F4FF` / border 1px `#013262` / 라벨 `#013262` / 체크 아이콘 |
| 10b | **hover/focus/error/disabled** 정본 | **(나)** | 전제 B 가 상태 전환을 1차에 포함시킴, 시안 전무 → **D5** |
| 11 | 입시 마스터 데이터 소스 | **(다)** | 데이터 확보는 기능. 단 **Q15 1차 렌더 형태**는 파생 블로커 → **D3** |
| 12 | 히어로 브라우저 목업 클립 | **(가)** | 권장 (a) 유지 — 원값 승계 원칙 정합, ver2 구현 관행 존재 |
| 13 | 4STEP 카드행 1180px | **(나)** | 전역 폭 정책이 (b)만 배제, (a)265축소 / (c)패딩축소 선택 잔존 + 시안 자체 오버플로 → **D8** |
| 14 | 헤딩 색 3종 | **(가)** | 원값 승계 원칙(재논의 금지)에서 (a) 자동 도출 |
| 15a | 명백한 오타 교정 | **(가)** | 권장 (b) — `피요한/힙격/경역/시급힌다/초근/정제되어/이예요` 교정, 끝 공백 제거 |
| 15b | 의미 변경 2건 | **(나)** | `사람마다`↔`과목마다`, Q3-C 라벨 Q3-B 중복 — 1차 렌더 텍스트가 확정 안 됨 → **D10** |
| 16 | Q8 하위칩 / Q3-C 단일·복수 | **(나)** | 라디오↔체크 아이콘·상태 전환이 달라져 1차 마크업이 갈림 → **D9** |
| 17 | Q15 셀렉트 placeholder 문구 | **(가)** | 권장대로 코드 문구 유지 — 시안 값(`건국대학교` 등)은 실데이터 예시 |
| 18 | 관리자 문항 편집 범위 | **(다)** | Admin UI = 기능, 전제 C |
| 19 | 조건부 노출 로직 12건 | **(가)** | 권장 (a) 무분기(시안 그대로 항상 노출)로 1차 확정. 단 "최대개수 초과 동작" 1건만 분리 → **D6** |
| 20 | 결과 리포트 착수 시점 | **(나)** | 신규 지시로 질문이 바뀜 — "1차 디자인에 12784 를 포함하는가" 범위 결정 → **D1** |
| 21 | 스키마 확장 경로 | **(다)** | 전제 C 명시 이관 |
| 22 | 로고 SVG 교체 주체 | **(라)** | 헤더/푸터 제외 → 이 브랜치에 로고 교체 작업 자체가 없음 |
| 23 | 일러스트 3종 최적화 수준 | **(가)** | 권장 (a) @2x 리사이즈. 3.5MB→약 400KB 로 충분, WebP 는 별도 이슈 |

집계 — **(가) 12건** / **(나) 7건** / **(다) 6건(7,8,9,11,18,21)** / **(라) 1건**
(#10·#15 는 a/b 분할되어 항목 수 25로 셈)

---

## 2. 2차 이관 명시 확인

지시받은 8개 항목 전부 **2차 확정**. 1차에서는 아래 대체물로 진행한다.

| 항목 | 원 결정# | 2차 확정 | 1차 대체물 |
|---|---|---|---|
| 스키마 확장 경로 | #21 | ✅ | 스키마 변경 0건. `src/data/renewalSurveyQuestions.js` 하드코딩 유지·확장 |
| 응답 저장 (submissions/answers) | #7 | ✅ | `useState` 클라이언트 state. 새로고침 시 소실 허용 |
| 채점 규칙 (6축·배점·컷오프) | #8 | ✅ | 채점 없음. 리포트는 시안 고정 수치 |
| PDF 출력 방식 | Phase 5-7 | ✅ | 버튼 마크업만(253×60 `#013262` r30), `onClick` 없음 |
| 입시 마스터 데이터 | #11 | ✅ | Q15 는 더미 옵션 배열(현행 `CascadingSelect` 유지), 입결 비교표는 시안 고정값 |
| 관리자 편집 범위 | #18 | ✅ | `Admin.jsx` 무변경. free_diagnosis CRUD 그대로 방치 |
| 서술 문단 생성 | #9 | ✅ | 시안 3문단 원문 하드코딩 |
| 결과 리포트 데이터 연동 | #20 파생 | ✅ | props 로 더미 객체 1개 주입, 라우트는 `/free-diagnosis/result` (`:id` 없음) |

**추가 2차 이관** (23건 밖, Phase 재배치분): 조건부 분기표 확보 후 반영(#19 (b)) · `depends_on_*` 컬럼 · `supabase/migrations/` 베이스라인 덤프 · Admin 응답 조회 화면 · 결과 URL 공유 · 인증 게이트 정책.

---

## 3. 1차 블로커 최종 목록 (10건)

> 23건 → 1차 블로커 10건. 이 중 **D1·D2 는 범위 경계**라 가장 먼저 답해야 한다.

### D1. 결과 리포트 `1889:12784` 를 1차 디자인 범위에 포함하는가 🔴 최우선

- **선택지**
  (a) 포함 — 더미 값으로 A4 2페이지 정적 렌더. 레이더/바그래프도 하드코딩 데이터로 SVG 동적 렌더
  (b) 포함하되 축소 — 정적 요소만(표·카드·서술), 차트 2종은 2차
  (c) 제외 — 1차는 랜딩 + 설문 5스텝까지. 리포트는 2차 UoW
- **권장: (a)** — 1차 정의가 "디자인 구현"이고 리포트는 시안 7노드 중 1개다. 채점 규칙 없이도 더미 값으로 픽셀 정합은 전부 검증 가능하며, 2차에서 값만 바꿔 끼우면 된다. `RadarChart6`/`DimensionBarChart` 는 래스터 도입 금지가 이미 확정이라 어차피 SVG 를 그려야 하고, 더미 값으로 그리는 비용과 실값으로 그리는 비용이 같다.
- **막는 작업**: Phase 5 전체 · `RadarChart6`/`PriorityTable`/`DimensionBarChart`/`AdmissionCompareTable`/`RecommendServiceCard`/`PdfDownloadButton` 6컴포넌트 · 라우트 `/free-diagnosis/result` 추가 여부 · 랜딩 입시좌표 맥북 화면 콘텐츠(리포트 A4-3 축소 렌더 대안이 D1에 종속)

### D2. 1차에서 제출 버튼("진단 결과 보기")은 무엇을 하는가 🔴 최우선

- **선택지**
  (a) `navigate('/free-diagnosis/result')` — 더미 리포트로 이동 (D1=(a)일 때만 성립)
  (b) 아무 것도 안 함 — `disabled` 해제만 되고 클릭 무동작
  (c) `console.log(answers)` + alert 스텁
- **권장: (a)** — 5스텝 전체 흐름이 화면으로 이어져야 디자인 검수가 성립한다. D1=(c)면 (b)로 폴백.
- **막는 작업**: `SurveySubmitButton` 신규 컴포넌트 · 스텝5 완주 플로우 · 라우팅 확정

### D3. Q15 캐스케이딩 셀렉트의 1차 렌더 형태

- **선택지**
  (a) 시안대로 셀렉트 4단 + **더미 옵션 배열**(주요 대학 5~10개 수준) — 현행 `CascadingSelect` 유지
  (b) 시안대로 셀렉트 4단 + 옵션 0개(항상 빈 드롭다운) — 마크업만
  (c) 자유 텍스트 4칸으로 임시 대체 (#11 권장 (d) 노선)
- **권장: (a)** — 1차 목적이 디자인 검수이므로 드롭다운이 열리고 선택 상태(chevron rotate 90 포함)가 보여야 한다. 더미 데이터는 2차에서 통째로 교체될 임시물임을 파일 상단에 주석 명시.
- **막는 작업**: `CascadingSelect` 수정 · 설문 스텝5 · 셀렉트 chevron 에셋 추출(`1889:10716`)

### D4. 최종 CTA "진단 결과 보기" 활성색 *(시안 미제공)*

- **선택지** (a) `#013262` 브랜드 · (b) `#0B84FD` accent 토큰 · (c) 디자이너 요청
- **권장: (a) `#013262`** — 랜딩 최종 CTA 버튼(300×68)과 리포트 PDF 버튼(253×60)이 모두 `#013262`. 계열 일치가 이미 확립.
- **막는 작업**: `SurveySubmitButton` · 스텝5 화면 완성

### D5. hover / focus / error / disabled 상태 정본 *(시안 미제공)*

- **선택지** (a) 자체 설계 후 디자이너 사후 확인 · (b) 디자이너 시안 선확보 · (c) 상태 없이 selected 만 구현
- **권장: (a)** — 전제 B가 상태 전환을 1차에 포함시켰고 selected 정본(`#E9F4FF`/`#013262`)이 이미 확정이라 파생 설계가 가능하다. 구체 제안은 §4.
- **막는 작업**: `OptionGroup`·`LikertMatrix`·`GradeInputGrid`·`CascadingSelect`·`ConditionalTextInput` 5컴포넌트 전부 (= 설문 5화면 전부)

### D6. 최대 선택 개수 도달 시 동작 (Q10 max3 ×2, Q14 max2) *(시안 미제공)*

- **선택지**
  (a) 현행 유지 — 미선택 항목 `disabled` + `opacity-50` + `cursor-not-allowed`
  (b) 클릭 무시하되 시각 변화 없음
  (c) 가장 오래된 선택을 자동 교체(FIFO)
  (d) 토스트/인라인 안내문 노출
- **권장: (a) + 헤더에 "N/3 선택" 카운터 추가** — 현행 `OptionGroup.jsx:42,56` 이 이미 (a). disabled 만으로는 이유가 전달되지 않아 카운터가 필요하다. (c)는 사용자가 의도치 않게 선택을 잃는다.
- **막는 작업**: `OptionGroup` multiple 경로 · 스텝3·4·5

### D7. 반응형을 1차에 포함하는가 · 리커트 모바일 적응 *(시안 미제공)*

- **선택지**
  (a) 1차에 포함 — 데스크톱 1920 정합 + 모바일 적응까지 동시에
  (b) 1차는 데스크톱만, 반응형은 1차-후반 별도 패스
  (c) 반응형 2차
- **권장: (b)** — 시안이 1920 단일이라 데스크톱 픽셀 정합을 먼저 못박고, 그 다음 적응한다. 다만 (c)는 안 된다(반응형은 디자인이지 기능이 아니다). 리커트 5열 매트릭스는 **모바일에서 문장 단위 카드 분해**(문장 1개 + 5점 라디오 가로 배치)를 기본 제안 — 가로 스크롤은 12행을 스크롤시키면 척도 헤더가 사라져 사용 불가.
- **막는 작업**: `LikertMatrix` 구조 결정(분해형이면 마크업 자체가 달라짐) · 992 고정폭 5화면 · 리포트 A4 1120 컨테이너

### D8. 4STEP 카드행 1180px 초과 + 카드 설명문 오버플로

- **선택지**
  (a) 1100 안에서 카드 265px 로 축소 (gap 20 유지)
  (b) 1180 유지 — 이 섹션만 컨테이너 예외
  (c) 카드 280 유지 + 좌우 패딩 30→20 축소로 설명문 1줄 확보
- **권장: (a) + 설명문 2줄 래핑 허용** — 전역 폭 정책이 확정 사항이라 (b)는 배제. (c)는 시안이 요구한 260px 1줄을 265−40=225px 로도 못 만들어 결국 래핑된다. 카드 높이 178px 은 2줄을 수용한다.
- **막는 작업**: 랜딩 Phase 2-4

### D9. Q8 하위 칩 · Q3-C 의 단일/복수 선택 여부

- **선택지** (a) 현행 유지(Q8 하위=복수, Q3-C=단일) · (b) Q8 하위를 단일로 정정 · (c) 기획 확정 대기
- **권장: (b)** — Q8 하위 칩에 "여러 과목" 선택지가 존재하는 것은 단일 선택 설계의 신호다. 복수 선택인데 "여러 과목" 옵션이 따로 있으면 의미가 중복된다.
- **막는 작업**: 스텝2(Q8) 마크업 — 라디오/체크 아이콘과 상태 전환 로직이 갈림

### D10. 오탈자 중 의미가 바뀌는 2건

- **선택지**
  (a) 원문 유지 (`사람마다 성적 변동`, Q3-C 라벨 = Q3-B 라벨과 동일)
  (b) 문맥 교정 (`과목마다 성적 변동`, Q3-C 라벨은 "선택 이유" 계열로 신규 작문)
  (c) 기획 확정 대기
- **권장: (b) 단 Q3-C 라벨 문구는 사용자 제시** — `사람마다`→`과목마다`는 Q8 성적 흐름 맥락상 명백. Q3-C 라벨 중복은 화면에 같은 문구가 2번 렌더되는 가시적 결함이라 1차에서 방치 불가.
- **막는 작업**: 스텝1(Q3 하위) · 스텝2(Q8) 문구 확정, `renewalSurveyQuestions.js` 19문항 확장 작업

---

## 4. 시안 미제공 항목 · 기본 제안

전역 정책(#3.1-5 accent 하드코딩 금지, selected 정본 `#E9F4FF`/`#013262`)에서 파생한 제안이다. 디자이너 사후 확인 대상.

### 4-1. 선택지 행/칩 (`OptionGroup`)

| 상태 | 제안 | 비고 |
|---|---|---|
| default | border `#D7D7D7` / bg `#FFFFFF` / 라벨 `#525252` | 확정(3.2-9) |
| **selected** | bg `#E9F4FF` / border 1px `#013262` / 라벨 `#013262` / `radio-checked.svg` | 확정(전제 3) — **현행 코드 `bg-[#F1F8FF]` 는 오류, `#E9F4FF` 로 교정 필요** |
| **hover** (미선택) | border `#013262` 20% 혼합 + bg `#FBFAFA`, `transition 150ms` | 현행 `border-[#013262]/40` 는 과함 |
| **hover** (선택됨) | 변화 없음 | 선택 상태가 이미 최상위 강조 |
| **focus-visible** | `outline 2px #0B84FD` + `outline-offset 2px` (accent 토큰) | 키보드 전용. `:focus` 아닌 `:focus-visible` |
| **disabled** (max 도달) | `opacity-50` + `cursor-not-allowed`, 색 변경 없음 | 현행 유지 |
| **error** | 행 그룹 하단 12px gap + `#D92D20` 12px 안내문, 행 border `#D92D20` | 1차엔 필수 미검증이라 스타일만 준비 |

### 4-2. 텍스트 입력 / 셀렉트

| 상태 | 제안 |
|---|---|
| default | border `#D7D7D7` r20 / placeholder `#D7D7D7` |
| hover | border `#B0B0B0` |
| focus | border 1px `#013262` + `outline 2px #0B84FD/30` |
| filled | 텍스트 `#181D24` |
| disabled (캐스케이드 선행 미선택) | bg `#F5F5F5` / 라벨 `#D7D7D7` / `cursor-not-allowed` |
| error | border `#D92D20` |

### 4-3. 최대 선택 도달 (D6)

- 질문 헤더 우측에 `2 / 3` 카운터 (14px, `#808080`; 도달 시 `#013262`)
- 미선택 칩 `opacity-50` + `disabled`
- 도달 상태에서 **선택된 항목은 계속 해제 가능**해야 함 (현행 코드 정상)

### 4-4. 최종 CTA (D4)

| 상태 | 제안 |
|---|---|
| disabled | bg `#D7D7D7` / 라벨 `#FFFFFF` (시안 원값) |
| **활성** | bg `#013262` / 라벨 `#FFFFFF` |
| hover | bg **`#01274D`** (=`#013262` 명도 −10%) |
| press | `#001C38` + `scale(0.99)` |
| loading | 스피너 16px + 라벨 "진단 결과 생성 중" · **2차에서만 사용** |

### 4-5. 반응형 (D7) — 기본 브레이크포인트 제안

| 뷰포트 | 규칙 |
|---|---|
| ≥1280 | 시안 정합. 카드 1112(=`max-w-content` 내부), 콘텐츠 992 |
| 1024~1279 | 컨테이너 유동, 카드 padding 60→40, 리커트 유지 |
| 768~1023 | 리커트 **문장 카드 분해**, 숫자 그리드 3열, 칩 wrap |
| <768 | 카드 radius 40→24, padding 20, 행 h68 유지·라벨 16px, 숫자 그리드 2열, 캐스케이드 셀렉트 세로 4단, 리포트 A4 1120 → 폭 100% 유동(고정 A4 비율 포기) |

리포트 PDF 는 2차이므로 A4 고정 레이아웃은 인쇄용 `@media print` 로 1차에서 분리 준비만 한다.

---

## 5. "1차 디자인 구현"의 정의

### 5-1. 데이터는 어디서 오는가

**`src/data/renewalSurveyQuestions.js` 하드코딩 유지 + 확장.** DB 접근 0건.

확장 내용 (전부 이 파일 안에서, 스키마 변경 없음):
- 12번 문항 편입 → **19문항** (현행 17 + Q10(9491 13지) + Q10(9866 14지) 정리)
- 각 문항에 `page: 1~5` 필드 추가 (분포 3/5/2/4/5)
- `max` 필드 정합 (Q10 ×2 = 3, Q14 = 2)
- `exclusive` 플래그 2건 (배타 선택지) — 클라이언트 state 로만 처리
- 조건부 `dependsOn` 은 **무시** (#19 (a) 무분기 = 항상 노출) — 필드는 남기되 렌더가 참조하지 않음
- 오탈자 교정 반영 (§1 #15a 확정분 + D10 확정 후)

리포트 더미는 `src/data/renewalReportSample.js` (신규) 1파일로 격리 — 2차에서 통째 삭제 가능하도록.

### 5-2. 제출 버튼은 무엇을 하는가

→ **D2 결정 대기.** 권장안 기준: `navigate('/free-diagnosis/result')`. Supabase insert 0건, 검증 0건, 로딩 상태 없음.
스텝 간 이동은 `useState` 를 상위 `SurveyStepLayout` 이 보유(라우팅은 `/survey/:step`, 상태는 메모리). 새로고침 시 답변 소실은 1차에서 **정상 동작**으로 간주한다.

### 5-3. 결과 리포트는 어떤 값으로 렌더되는가

→ **D1 결정 대기.** 권장안 기준: **시안 12784 의 표시값을 그대로 더미 상수화**해서 렌더. 즉
- 레이더 6축 점수 = 시안 도형에서 역산한 6개 값 (축 순서·최대값은 개발이 정하고 2차에 재검토)
- 우선순위표 진행바 fill = **시안 픽셀값 그대로** (본문 서술과 불일치하나 1차는 시안 재현이 목표)
- 학생명·학년·진단완료일 = `"홍길동" / "고등학교 2학년" / "2026.07.29"` 고정
- 서술 3문단·강점/보완 불릿·입결 비교표 = 시안 원문 (오타 교정분 반영)
- PDF 버튼 = 마크업만, 무동작

### 5-4. 경계가 모호해 결정으로 승격한 것

D1(리포트 포함 여부) · D2(제출 동작) · D3(Q15 렌더 형태) · D7(반응형 포함 범위). §3 참조.

---

## 6. 작업 항목 재분류

### 1차 (디자인)

1. 에셋 — 일러스트 3종 @2x 크롭/리사이즈 (3.5MB→약 400KB)
2. 에셋 — 재추출 **4건**: 셀렉트 chevron(`1889:10716`) / 맥북 Shadow(`2240:4580`) / 맥북 Bottom(`2240:4588`) / 얻을것 아이콘 3종은 컨테이너 통째 @3x (`2162:1146`·`1155`·`1163`) ※ 로고 2건은 (라)로 제외
3. 에셋 — 체크박스(`1889:9733`) 바이트 비교 후 (A)/(C) 판정
4. 에셋 — 고아 2건(`macbook-mockup.png`, `macbook-screen-content.png`) 삭제
5. 랜딩 `2162:1020` 전 섹션 — 리듬 정규화(pt 소유·pb=0, 본문 오프셋 `pt-[5.25rem]`) / 헤딩 스케일 32px·600(최종 CTA 만 44px·700) / 히어로 정합 / 4STEP(D8) / 추천대상 / 얻을것 / 입시좌표 맥북 **CSS 재현** + 칩 3개 컨테이너 기준 재배치 / 최종 CTA
6. `renewalSurveyQuestions.js` 19문항 확장 (§5-1)
7. `SurveyStepLayout` 신규 + 라우팅 `/free-diagnosis/survey/:step` (redirect 포함)
8. `ConditionalTextInput` 에 `multiline`/`rows` prop 추가 (Q19)
9. `SurveySubmitButton` 신규
10. 설문 5스텝 구현 — 카드/행/배지 공통 규격(1112·r40·pad 60/40·콘텐츠 992·gap 40/60) + line-height 1.4~1.5 예외 적용
11. 칩 전환 (Q13/14/16/17/18) `flex-wrap`
12. 리커트 폭 정규화 (992 기준, 균등 grid 로 척도↔라디오 중심 정렬)
13. 상태 전환 일괄 — selected 정본 적용(**`#F1F8FF`→`#E9F4FF` 교정**) + hover/focus/disabled (D5)
14. 배타 선택지 2건 + 최대 선택 개수 처리 (D6)
15. 결과 리포트 `12784` 정적 렌더 + 차트 SVG (D1 = (a) 시)
16. 반응형 적응 패스 (D7 = (b) 시 1차 후반)

### 2차 (기능)

1. `supabase/migrations/` 베이스라인 덤프 + 스키마 확장 마이그레이션 (#21)
2. `free_diagnosis_submissions` / `_answers` 신설 (#7)
3. `renewalSurveyQuestions.js` → DB 시드 이전 (또는 코드 상수 유지 재확인)
4. 응답 저장/조회 API 레이어 · 스텝 상태 영속화 · 새로고침 복구
5. 채점 규칙 구현 — 6축 정의 / 문항→축 매핑 / 리커트 배점 / 컷오프 / 진행바 매핑 (#8)
6. 리포트 실데이터 연동 (`/result/:id`) + 더미 파일 삭제
7. 서술 3문단·강점/보완 불릿 생성 (#9, 룰 기반 템플릿 우선)
8. PDF 출력 (클라이언트 렌더 vs 서버 생성 결정 포함)
9. 입시 마스터 데이터 확보 + Q15 실옵션 + 입결 비교표 (#11)
10. 조건부 노출 분기표 반영 (#19 (b)) + `depends_on_*` 컬럼
11. 관리자 축소안 — 문구만 편집 (#18) + Admin 응답 조회 화면
12. 인증 게이트 정책 · 결과 URL 공유 · `user.id` 연결

### (라) 무효화 — 이 브랜치에서 하지 않음

- 로고 SVG 교체 (#22) — 헤더 로고 `2240:2714` / 푸터 로고 `2240:3273` 추출 포함
- 64px 헤더 도입 및 `pt-16` 전역 리듬 (3.1-4) — 현행 84px 유지, `pt-[5.25rem]`
- 신규 푸터 / 푸터 밴드 100% 폭 처리 (3.2-13)
- `landing-renewal` 선행 머지 대기 (SPEC Phase 0-2, 0-3) · 리스크 R4

---

## 7. 리스크 재평가

| # | 리스크 | 1차 영향 | 2차 영향 |
|---|---|---|---|
| R1 | 입시 마스터 데이터 부재 | 낮음 — D3 더미로 흡수 | **높음 유지** |
| R2 | 채점 규칙 미정 | 낮음 — 더미 렌더 | **높음 유지** |
| R3 | 응답 저장 전무 | 없음 — 1차 범위 밖 | 높음 |
| R4 | 브랜치 의존(헤더/푸터) | **해소** — 전제 A | 머지 시 본문 오프셋 84→64 재조정 필요 |
| R5 | 상태 디자인 전무 | **높음 → D5 로 해소 예정** | — |
| R6 | 반응형 시안 전무 | **중간 → D7** | — |
| R7 | 조건부 로직 미정 | 낮음 — 무분기 확정 | 중간 (데이터 모델 되돌림) |
| R8 | 시안 자체 결함 | 중간 — D8·D9·D10 로 흡수 | — |
| R9 | 관리자 CRUD 전제 붕괴 | 없음 | 중간 |
| R10 | 에셋 URL 충돌 | 낮음 — 컨테이너 노드 추출로 회피 | — |
| R11 | 번들 크기 | 낮음 — 1차 항목 1 | — |
| R12 | 마이그레이션 이력 부재 | 없음 — 스키마 무변경 | 중간 |

**신규 R13** — 현행 코드 `OptionGroup.jsx:70` 의 selected 배경이 `#F1F8FF` 로 확정 정본 `#E9F4FF` 와 불일치. 1차 항목 13에서 반드시 교정.
**신규 R14** — `SurveyPreview.jsx` 가 `fixed` 헤더 오프셋(`pt-[5.25rem]`)을 갖고 있지 않음. 랜딩(`FreeDiagnosisLanding.jsx:308`)은 있음. 스텝 레이아웃 신설 시 반영.
