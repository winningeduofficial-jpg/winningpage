# Dependencies
node_modules/
# 워크트리에서는 node_modules를 실제 디렉터리가 아니라 심볼릭 링크로 둔다
# (../winningpage-dev/node_modules). 위 트레일링 슬래시 패턴은 디렉터리만
# 매칭하고 심볼릭 링크는 매칭하지 않아 git status에 untracked로 뜬다 —
# 파일 타입 무관하게 이름으로 매칭하도록 슬래시 접두 패턴을 추가한다.
/node_modules

# Build output
dist/
dist-ssr/
build/

# Vite / cache
.vite/
*.local

# Environment variables (secrets)
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Supabase local temp
supabase/.temp/
.vercel

# seed-landing-storage.mjs 산출물 (환경별 URL 매핑)
landing-url-map.json

# 작업용 명세·분석 문서 (커밋하지 않는다 — 사용자 확정 2026-07-30)
docs/

# Playwright MCP 브라우저 검증 산출물 (스냅샷·콘솔 로그)
.playwright-mcp/

# 대입모집요강 HTML 골든 전문 캐시 — tests/fixtures/admission-html-golden.json(해시)의
# 로컬 디버깅용 원본. scripts/build-admission-html-golden.mjs로 언제든 재생성 가능하므로
# 커밋하지 않는다.
/.golden-cache/

spike/
