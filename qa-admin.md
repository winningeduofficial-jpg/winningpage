# QA시트 — 관리자웹 50건 dev 반영 대조

- 대조 기준: `origin/dev` @ `3d824a36` (2026-08-29)
- 대상: 시트 전체에서 카테고리에 「관리자웹」이 포함된 행 = **50건** (204행 이전 15건 + 204행 이후 35건)
- 배경: 위닝측이 **운영 페이지**(개발분 미반영)를 보고 작성해 dev 에 이미 들어간 건이 섞여 있음
- **작업 대상은 dev DB(`gjowqdiopinhixfivnkx`) 하나뿐이다.** 개발 완료 후 prod 로 일괄 이관하므로 운영 확인은 하지 않는다 — 운영에서만 재현되는 건은 이 문서에서 다루지 않는다

| 판정 | 건수 |
|---|---|
| ✅ dev 반영 완료 | 5 |
| 🟡 부분 반영 / 사양 차이 | 3 |
| ⏸ 위닝측 보류 표기 (콜멘토 계열) | 6 |
| 📨 개발 아님 — 회신으로 종결 (336·338·31) | 3 |
| ❌ 미반영 | 33 |

---

## 1. ✅ 이미 반영 — 위닝측에 "dev 확인 요청"으로 회신

| 행 | 요청 | dev 근거 |
|---|---|---|
| 149 | 프리미엄 문의 취합 관리자 메뉴 | `configs/main.ts:619 premiumConsults` (시트도 최종 TRUE) |
| 178 | 권한을 메뉴별 선택형으로, 대표그룹 + 개별 추가/제외 | `AdminMembersAdmin.tsx` 「개별 권한 설정」 탭 + `fn_admin_effective_permissions`(묶음 ∪ 개별 합산을 화면에 노출). 사이드바·라우트 적용까지 완료 (커밋 2026-08-24) |
| 182 | 「고객조회상담」 메인메뉴 + 서브 4개 | 회원관리 상세 6탭이 통째로 흡수 (`Admin.tsx:223` 주석에 QA 182 명시). **단 357(알림톡 발송)은 별건 미반영** |
| 220 | 멘토 얼굴 좌→우 자동 흐름 | `MentorSection.tsx` + `useInfiniteMarquee` (rAF 자동 스크롤 35px/s) |
| 224 | 게시노출수 수기 조정 칸 | `configs/board.ts` — 교육컬럼·공지사항·회사소식 3개 모두 `view_count "조회수 조정"` 필드 존재 |

## 2. 🟡 부분 반영 / 사양 차이

| 행 | 요청 | 현재 | 갭 |
|---|---|---|---|
| 215 | 등록한 학과가 입결 하단에 안 뜸 | 뜨는 학과 칩 노출 수정 완료 (`AdmissionResults.tsx`, 2026-08-25) | 334행(칩 클릭 → 입결 상세 이동)은 미반영 |
| 219 | 핵심서비스에 추가서비스 3개 등록 | 6개 상한·폴백 제거 (`ServicesSection.tsx`, 2026-08-25) | 성장설계/프리미엄1/프리미엄2 실제 등록·링크 미반영. 289행이 후속 정정 |
| 186 | 회원구분 코드 항목 추가 | 가입 시 **소속코드** 입력 추가됨 (213행, 2026-08-25) | 관리자 회원목록에 컬럼으로 안 나옴 → 226행과 한 덩어리 |

## 3. ⏸ 위닝측이 "보류"로 표기한 건 (손대지 말 것)

30, 141, 142, 180, 181, 183 — 전부 콜멘토 관리자 화면 계열. 290행에서 **콜멘토 런칭이 10월 이후로 연기**됐으므로 이 6건은 계속 보류.

---

## 4. ❌ 미반영 36건 — 작업 묶음

### A. 개인정보 다운로드 게이트 (5건) — 한 컴포넌트로 묶어 처리
**268 / 270 / 223 / 228 / 271**

요구가 사실상 하나입니다: *다운로드 버튼 → 관리자 비밀번호 입력 + 다운로드 사유 필수 기재 → 로그 저장*.

| 행 | 대상 | 현재 |
|---|---|---|
| 268 | 회원 목록 전체 | 다운로드 없음 |
| 270 | 멘토 신청 내역 | `configs/member.ts:150 mentorApplications` 에 `excel` 없음 |
| 228 | 멘토 신청 내역 (270의 선행 요청) | 위와 동일 |
| 223 | 프리미엄 상담 신청 내역 | `configs/main.ts:627` 에 **개인정보 사유로 CSV를 의도적으로 비활성**해 둠 — 게이트가 생기면 그걸 붙여서 여는 게 맞음 |
| 271 | 매출 및 결제 | `configs/revenue.ts:90` custom 컴포넌트, `RevenueAdmin.tsx` 에 다운로드 없음 |

동반: **269** 마스킹 해제도 같은 게이트(비번 + 로그)를 요구 → `MembersAdmin.tsx:496` 은 현재 단순 토글.
→ **비번 확인 + 사유 입력 + 로그 적재를 공용 모달 1개로 만들고 6곳에 붙이는 게 최소 작업**입니다. 로그 테이블(`admin_access_logs` 류)은 신규 마이그레이션 필요.

### B. 업로드 전면 불가 (4건) + 멘토 데이터 (2건) — **dev 실측으로 대부분 배제됨**
**314 본문 이미지 / 315 모바일 이미지 / 316 첨부파일 / 222 프리미엄 책자 / 336 멘토 신청 내역 비어있음 / 318 증빙 Object not found**

넷 다 Supabase Storage 쓰기 경로이고 정책이 `is_winning_admin()` 게이트(`20260821000001_storage.sql:23`)라,
초대 관리자의 `profiles.role` 이 `user` 로 남는 버그(8/23 커밋 `49cf1eca`, 백필 마이그레이션 없음)를
공통 원인으로 의심했으나 **dev 실측 결과 성립하지 않는다.**

**dev DB(`gjowqdiopinhixfivnkx`) 실측 (2026-08-31):**

| 확인 항목 | 결과 |
|---|---|
| `admin_members` | 2명, 둘 다 `status='active'` |
| 두 계정의 `profiles.role` | 둘 다 `'admin'` → `is_winning_admin()` = **true** |
| `banners/admin/` 최근 객체 | **2026-08-26 04:38** 업로드 성공 (QA 314·315 보고 **다음날**) |
| `banners/premium-book/booklet.pdf` | created 08-07, **updated 2026-08-25 14:07** (QA 222 보고 3일 뒤 재적용 성공) |
| `banners/notice-files/` | **완전히 비어 있음 — 첨부파일이 한 번도 올라간 적 없음** |
| `mentor_applications` | **0행** |
| `mentor-applications` 버킷 | 비어 있음 |

정책이 아예 없었다면 8/26 이미지 업로드부터 실패했을 것이므로, storage RLS 는 dev 에서 정상 동작 중이다.

**따라서:**
- **314 · 315 · 222** → dev 에서 동작 확인. **운영 미반영 탓일 가능성이 높다** (1절 성격). 운영에서 재확인 필요
- **316 첨부파일** → dev 에서도 성공 이력 0건. **유일하게 살아 있는 진짜 후보.** 재현 우선
  (경로: `AdminEngine.tsx:1955` → `banners/notice-files/`, 필드는 `configs/board.ts:109·178`·`admission.ts:1167` 의 `type:"multiFile"`)
- **336 · 318** → dev 에 멘토 신청 데이터가 0행이라 **재현 자체가 불가**. 운영 DB 확인이 선행돼야 함

**dev 실측 확정 (2026-08-31, 사용자 SQL 재확인 완료):**
```
email                  role    status        |  select count(*) from mentor_applications
devadmin@gmail.com     admin   active        |  0
rantae4767@gmail.com   admin   active        |
```

**336 은 버그가 아니다 — 회신 사항.** 신청은 **운영 사이트(winningedu.com)** 로 들어오고
prod `mentor_applications` 에 정상 적재되고 있다. dev 가 0행이었던 건 프리뷰 URL 로 실제
지원자가 오지 않기 때문이다. 배선은 dev·main 이 동일하다:

- GNB 「이용신청 > 멘토신청」 ← `page_contents.slug='mentor-apply'` (dev 도 `is_active=true`)
- `/page/mentor-apply` → `/mentor-apply` (`applyRoutes.tsx:16`) → `MentorApplyForm`
- ① `POST /api/mentor-apply-upload-url` (증빙 signed URL) → ② `POST /api/mentor-apply`
  → `createSupabaseAdmin()` service role insert
- `main ↔ dev` diff 는 공통 handler 계층 이관 **리팩터링뿐**, insert 경로 동일
- dev 배포 번들이 붙는 Supabase = `gjowqdiopinhixfivnkx` (**dev DB 맞음**)

**cross-write 도 아니다** — dev `phone_verifications` 에 멘토 관문 purpose 인
`mentor_apply`(`api/mentor-apply.ts:150`) 가 **0건**이다. dev 에서 폼을 끝까지 진행한
사람이 아예 없다는 뜻이라, "인증은 dev 에 남고 신청만 prod 로 갔다"는 시나리오가 배제된다.

**dev 확인용 합성 데이터 5건 투입 (2026-08-31).** 실데이터 복제는 하지 않았다 —
`supabase/README.md` 의 "시드 화이트리스트에 유저 데이터 테이블 추가 금지" 및
`seed-from-dev.mjs:12` 방침에 어긋나고, 지원서에는 실명·휴대폰·증빙(성적표)이 들어 있다.
대신 상태값 6종 중 5종(`submitted`/`screening`/`interview`/`active`/`rejected`)을 덮는
합성 행을 dev 원격에 직접 insert 했다. 이름 `테스트멘토*`, 전화 `0100000000N`,
메일 `qa-mentor N@winning.test`(seed.sql QA 계정과 같은 비라우팅 도메인).

⚠️ `proof_file_path` 는 실제로 없는 경로(`qa-seed/N-synthetic-proof.pdf`)를 가리킨다 —
**의도적이다. 318(증빙 열람 `Object not found`)이 이 데이터로 그대로 재현된다.**

정리(데모 끝난 뒤):
```sql
delete from public.mentor_applications where email like 'qa-mentor%@winning.test';
```

**남은 작업은 사실상 2건:**

| 행 | 상태 | 다음 행동 |
|---|---|---|
| 316 | dev 에서도 성공 이력 0건 — **유일한 진짜 후보** | `banners/notice-files/` 업로드 재현. 필드 `type:"multiFile"` (`configs/board.ts:109·178`, `admission.ts:1167`) → `AdminEngine.tsx:1955` |
| 318 | 문구 교체 (독립 처리 가능) | `MentorApplicationsAdmin.tsx:245` 는 경로가 null 인 경우만 처리. storage 객체 부재(`Object not found`) 케이스를 같은 문구로 흡수. 336 과 무관하게 지금 고칠 수 있음 |

**닫아도 되는 건 (dev 동작 확인됨):** 314 · 315 · 222.
`banners/admin/` 8/26 04:38 업로드 성공, `banners/premium-book/booklet.pdf` 8/25 14:07 갱신 성공.
위닝측이 운영을 보고 적은 건이므로 dev 재현부터 하고, 안 되면 닫는다.

### C. 메뉴 구조·법률 (2건)
- **230 「위닝 생기부 DB」 메뉴 삭제** — 생기부 저장이 26.7.29부터 불법이라는 근거. `configs/winning.ts:224`, `Admin.tsx` 서비스관리 그룹, `20260822000010_admin_permissions.sql:407`, `20260823000002_admin_resources_recategorize.sql:68` 4곳 잔존. **법률 이슈라 단독 최우선.**
- **338 위닝DB/기초데이터 용도 문의** — 개발 아님, **회신만 하면 되는 질문**.
- **336 멘토 신청 내역이 비어 있음** — 멘토 신청을 **수기로 접수** 중이라 폼 제출분이 0행인 것이 정상. 개발 아님, **회신으로 종결**.

### D. 목록 항목 보강 (3건)
- **226** 회원 목록에 생년월일·소속코드·학교명·지역·이용서비스 추가 → `configs/member.ts:80~93` 에 없음. 생년월일·소속코드는 **DB에 이미 있음**(`20260825093735_signup_identity_fields.sql`)이라 컬럼만 얹으면 됨
- **272** 수강 신청 내역에 결제방식·승인번호·전화번호 추가 + 좌측 상단 「초기화」 버튼 삭제
- **227** 수강 신청 내역 상단에 서비스 선택 필터 + 항목을 회원목록과 동일하게

### E. 환불·정산 (4건) — 파일18·파일23 참조 필요
- **273** 환불 수기 대장 / 환불 신청 내역 재기획 + 환불신청일·환불처리일자 컬럼
- **275** 환불 신청 내역을 파일18 HTML 「환불처리대장」과 동일하게
- **274** 매출·결제관리에 총괄 통합 대시보드 (파일18 HTML 기준)
- **313** 정산관리 (파일23 zip — 기획서 + 기능요구명세서 + 참조HTML)

현재 메뉴는 `Admin.tsx` 매출·결제관리 그룹에 `refunds`(수기 대장) / `refundRequests`(고객 신청 원장) 둘 다 존재하고 각각 `excel: true`. 껍데기는 있고 **항목·대시보드가 파일18 사양과 다름**.

### F. 그 외 개별 (8건)
| 행 | 내용 | 메모 |
|---|---|---|
| 216 | 메인배너 노출 전환 시간 설정 칸 | `configs/main.ts:199` 6초 하드코딩. "현재 배너가 아예 안 바뀐다"는 지적도 포함 |
| 217 | 우측 소형배너 동일 | |
| 221 | 세부 페이지 관리 저장 시 재확인(변경 비밀코드) | 랜딩 메뉴 위치를 바꾸는 화면이라 요청 |
| 225 | 멘토신청 FAQ 일반/공지 구분, 공지는 항상 최상단 | `configs/board.ts:345` 에 `is_pinned` 없음. 공지사항·회사소식엔 이미 있으니 그 패턴 복사 |
| 229 | 일일 입장/이용 현황을 **로그 기반**으로 | 현 `configs/program.ts:30 dailyEntries` 는 이름·프로그램·클래스를 손으로 적는 수기 장부. 요구는 "일시-서비스명-접속자수-남녀비율-학생/학부모구분" 자동 집계 → **사실상 신규 개발** |
| 317 | 상세 → 뒤로가기 시 엉뚱한 메뉴로 이동 | |
| 318 | 증빙 파일 "Object not found" 문구 교체 | `MentorApplicationsAdmin.tsx:245` 는 경로가 **null 인 경우만** 처리. storage 객체 부재 케이스가 그대로 노출 |
| 337 | 순서 1 지정 시 나머지 자동 재배열 | `sort_order` 전반 공통 동작 변경 |
| 335 | 멘토스 소개 등록 실패("필수 값이 비어 있습니다") | `AdminEngine.tsx:796` required 검사. 재현 후 어떤 필드가 걸리는지 확인 필요 |
| 357 | 이름·전화번호·소속코드로 조회 후 1:1 / 1:다수 / 그룹 알림톡 발송 | 182 로 만든 회원 상세에 발송 기능을 얹는 형태 |

### G. 관리자웹 태그지만 실제로는 타 영역 (3건)
- **31** 매출정산에 토스페이먼츠 결제완료 자동 연동 — 「매출 및 결제」가 `admin_revenue_items` 뷰(orders/order_items) 기반으로 대체됨. **요구가 아직 유효한지 위닝측 확인 필요**
- **276 / 185** 쿠폰 3종(선물쿠폰 코드발행기 + 단체쿠폰) — `CouponAdmin.tsx` 에 해당 UI 없음. 185가 8/20, 276이 8/24 재요청
- **184** 회원관리에 「일반상담문의및회신」 서브메뉴 (게시판형 문의-답변, 4자리 비번으로 답변 열람)
- **311** 마이페이지 현금영수증 발급 버튼 — 마이페이지 작업

---

## 착수 순서 제안 (dev 기준)

1. **230** 생기부 DB 메뉴 삭제 — 법률, 30분
2. **316** 첨부파일 업로드 재현 — dev 성공 이력 0건인 유일한 건
3. **318** 증빙 파일 문구 — `Object not found` 케이스 흡수 (소품). 위 합성 데이터로 바로 재현됨
4. **회신만 하면 되는 건**: **336**(운영으로 정상 적재 중, dev 는 지원자가 없을 뿐), **338**(위닝DB 용도 질문), **31**(요구 유효성 재확인)
5. **A묶음** 다운로드/마스킹 게이트 공용 모달 1개 + 로그 테이블 → 268·270·228·223·271·269 여섯 건
6. **D묶음** 목록 컬럼 보강 226·272·227 — DB에 값이 이미 있어 저비용
7. **225 / 216·217 / 221 / 317 / 337 / 335** 개별 처리
8. **E묶음** 환불·정산 273·274·275·313 — 파일18·파일23 확보 후 별도 스프린트
9. **229**(로그 집계), **357**(알림톡 발송), **276**(쿠폰 3종) — 신규 개발 규모

**314 · 315 · 222** 는 dev 동작이 확인돼 목록에서 뺐다.
