# sql/ 실행 순서

Supabase SQL Editor에서 **파일명 접두어 순서대로** 실행합니다. 모든 파일은 idempotent(여러 번 실행해도 안전)하게 작성되어 있습니다.

| 순서 | 파일 | 역할 | 의존성 |
|---|---|---|---|
| 00 | `00_base_schema.sql` | 운영(ref `ucjlcvqvinspmrasvsug`) 스키마 스냅샷 — Supabase Management API 추출본, 2026-07-27 기준, public 스키마 테이블 55종: `profiles`, `program_categories`, `home_mentor_strategies`, `banners`, `learning_diagnosis_*` 등. dev/신규 프로젝트에 운영과 동일한 베이스를 재현한다 (스키마 전용 — 시드 없음) | 없음 (최초 실행) |
| 10 | `10_pricing_orders.sql` | 결제 스키마: `products`, `coupons`, `orders`, `order_items`, `refund_requests` + RLS + 상품/쿠폰 시드 | `auth.users`만 의존 (00과 독립적으로도 실행 가능) |
| 20 | `20_landing_renewal.sql` | 랜딩페이지 리뉴얼 스키마: `public.is_admin()` 헬퍼, `university_acceptances` 신규 테이블 + `program_categories.icon_image_url` 컬럼 + `home_mentor_strategies` 시드 | `00_base_schema.sql`의 `profiles`(role 컬럼), `program_categories`, `home_mentor_strategies` 필요 |
| 30 | `30_landing_admin_media.sql` | 랜딩 이미지 관리자 전환: `home_mentor_strategies` 카드 분해 컬럼 5종(badge/title_lines/photo_url/photo_layout/card_width) + 멘토 22건 백필 + `banners` 히어로 969×429 시드 + 구 규격 배너/레거시 멘토 행 정리 (전부 마커 가드) | `00_base_schema.sql`의 `banners`, `20_landing_renewal.sql`의 `schema_migrations` 마커·멘토 시드 필요. **업로드 스크립트(`scripts/seed-landing-storage.mjs`)보다 먼저 실행** |
| 31 | `31_storage_policies.sql` | Storage 정책: `banners` 버킷 public 보정 + `storage.objects` public read/`is_winning_admin()` write 정책 + `university_acceptances` write 판정 `is_winning_admin()` 교체 + `banners` 구식 profiles 서브쿼리 정책 4개 drop | `00_base_schema.sql`의 `is_winning_admin()`, `20_landing_renewal.sql`의 `university_acceptances` 필요 |
| 32 | `32_news_categories.sql` | 랜딩 "소식" 섹션 카테고리 배지(Figma 1907:14893): `company_news`/`notices`에 nullable `category` 컬럼 추가 + dev 테스트 행(Test1~3) 시드 UPDATE(마커 가드) | `00_base_schema.sql`의 `company_news`/`notices` 필요 |
| 33 | `33_drop_mentor_legacy_image.sql` | 멘토 성공전략 구버전 통이미지 완전 제거: `home_mentor_strategies.image_url` 컬럼 drop (되돌릴 수 없음 — 실행 전 백업 권장) | `30_landing_admin_media.sql`의 카드 분해 컬럼(badge/title_lines/photo_url 등)으로 전환 완료된 상태 필요 |
| 34 | `34_menu_navigation_sync.sql` | 헤더/푸터 공용 메뉴(`page_contents`) 트리를 Figma 2016:1796과 동기화: `menu_label` 8건 정정 + 서비스 그룹 노출 제외 2건(`menu_group = ''`) + 서비스 그룹 `sort_order` 재정렬 + `services-self-assessment`(자기평가) 신규 row 삽입. 전부 `is distinct from` 가드 / `on conflict (slug) do nothing` | `00_base_schema.sql`의 `page_contents` 필요 |
| 35 | `35_landing_service_card_links.sql` | `program_categories.link`를 스텁 `/services` → 실제 페이지 경로로 교체 (name 기준 VALUES 조인, `is distinct from` 가드로 재실행 no-op) | `20_landing_renewal.sql`의 `program_categories` 시드 필요 |
| 40 | `40_auth_signup.sql` | **로그인/회원가입 백엔드 누적 파일.** 회원유형 3종 제약, 약관/동의 이력, 학생 연결코드, 학부모-자녀 연결, 휴대폰 인증, `complete_signup_profile` 확장, 연결 RPC 4종, 가입 이어가기 판정, 연결코드 조회 이력, 본인확인 결과 | `00_base_schema.sql`의 `profiles`·`is_winning_admin()`·`extensions.pgcrypto`, `20_landing_renewal.sql`의 `schema_migrations` |
| 36 | `36_column_renewal.sql` | 교육칼럼 리뉴얼: `galleries`에 category/view_count/published_at/is_featured 컬럼 추가 + published_at 백필 + 조회용 인덱스 3종 (idempotent) | `00_base_schema.sql`의 `galleries` 필요 |
| 37 | `37_column_block_content.sql` | 교육칼럼 블록 에디터: `galleries`에 `content_json jsonb` 컬럼 추가 (idempotent). `content`(text)는 평문 미러로 계속 유지 | `00_base_schema.sql`의 `galleries` 필요. `36_column_renewal.sql`과 독립 실행 가능 |
| 38 | `38_admission_universities.sql` | 대학별 모집요강 이관 스키마 보강: 신규 `admission_universities`(지역별 대학 목록 마스터) 테이블 + is_active/sort_order 인덱스 + public read/`is_winning_admin()` admin write 정책, `admission_university_resources`에 HTML 3종 컬럼(`previous_year_changes_html`/`selection_method_html`/`exam_schedule_html`) 추가, 같은 테이블의 admin write·public read 정책을 `is_winning_admin()` 기준으로 문서화·통일 | `00_base_schema.sql`의 `admission_university_resources`, `is_winning_admin()` 필요 |
| 39 | `39_admission_resource_index_view.sql` | 대입모집요강 목록 fetch 장애(statement timeout 57014) 수정: `admission_university_resources` raw 6종+html 6종 본문(행당 수십 KB, 218행 합계 5MB+)을 목록에서 걷어낸 경량 뷰 `admission_university_resource_index` 신설(`security_invoker = true`로 기반 테이블 RLS 상속) — 경량 컬럼 + 카테고리별 `has_*` 존재 여부 불리언 12종만 노출. 본문은 모달 오픈 시 `admission_university_resources`에서 id 기준 on-demand 조회(프론트, `AdmissionGuidelines.jsx`) | `00_base_schema.sql`의 `admission_university_resources`(PostgreSQL 17 `security_invoker` 뷰 옵션 필요) |
| 40 | `40_admission_case_content.sql` | 수시정시 합격사례 블록 에디터: `admission_posts`에 `content_json jsonb` 컬럼 추가 (idempotent). 본문을 BlockNote 블록 JSON으로 저장하며, 기존 `content`(text)는 평문 미러로 계속 유지해 렌더러 롤백 시에도 본문이 출력되도록 한다 | `00_base_schema.sql`의 `admission_posts` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 41 | `41_admission_case_hero.sql` | 수시정시 합격사례 히어로 어드민 전환: `admission_acceptance_rates`(연도별 합격률, year UNIQUE + 2021~2025 5개년 시드 `on conflict do nothing`) / `admission_case_logos`(히어로 대학 로고 스트립, `display_height_rem`·`opacity` 개별 지정, 시드 없음 — 프론트가 번들 로고 12종으로 폴백) 신규 테이블 2종 + is_active/sort_order 인덱스 + public read/`is_winning_admin()` admin write 정책 | `00_base_schema.sql`의 `is_winning_admin()` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 42 | `42_admission_case_logo_row.sql` | 히어로 대학 로고 표시 줄 수동 지정: `admission_case_logos`에 `row_no smallint`(1 또는 2, 기본 1) 컬럼 추가 + range check 제약 + 조회용 인덱스. 기존 균등 절반 분할(`Math.ceil(n/2)`) 대신 로고별로 1행/2행을 편집 판단으로 지정해 시안(1882:6520)의 7/5 비대칭 구성을 재현한다 | `41_admission_case_hero.sql`의 `admission_case_logos` 필요 |
| 43 | `43_admission_results.sql` | 입결 데이터 통합: `admission_susi_results`(long)와 `admission_jungsi_results`(wide, 연도가 컬럼)가 같은 도메인을 두 포맷으로 저장하던 문제를 해소하기 위해 단일 long 테이블 `admission_results` 신설(`recruitment_period` '수시'/'정시' 축) + 중복 적재를 막는 표현식 유일성 인덱스 + 상세/셀렉터 인덱스 2종 + 집계 뷰 2종(`admission_result_university_index`/`admission_result_department_index`) + RLS(public read `is_active=true` / admin write) + `updated_at` 트리거(공용 `set_updated_at()` 재사용) + "지금 뜨고 있는 학과" 큐레이션 테이블 `trending_departments` + RLS. 레거시 두 테이블은 로컬·dev·운영 전부 0행(이관 대상 없음)이라 DROP하지 않고 `comment on table`로 deprecated만 표기하며, 개발 과정에서 레거시 테이블에 잘못 붙였던 인덱스 2종·쓰기 정책 1종과 구 뷰(`susi_university_index`/`susi_department_index`)를 정리(drop)한다. **⚠️ `recruitment_period`('수시'/'정시') 축과 셀렉터 인덱스 `admission_results_selector_idx`는 `54_admission_results_2yr.sql`에서 제거됐다** — 원본 자료에 모집시기 개념이 없어 값이 항상 '수시' 하나뿐이었다. 54를 함께 실행하면 이 파일이 만든 뷰 2종·유일성 인덱스는 54의 정의로 대체되므로, 이 행의 설명은 54 이전 상태 기준으로 읽을 것 | `00_base_schema.sql`의 `admission_susi_results`/`admission_jungsi_results`/`set_updated_at()`/`is_winning_admin()` |
| 43 | `43_learning_diagnosis_rename.sql` | 무료진단 → 학습진단 리네이밍: `free_diagnosis_{questions,options,programs}` 3종의 테이블·제약·인덱스·트리거·정책 rename + 값 갱신(`program_categories.name`/`link`, `page_contents.slug`/`menu_label`/`title`, `banners.button_link`). **dev(`gjowqdiopinhixfivnkx`, 서울) 전용 — 운영 DB에는 이 파일을 개별 적용하지 않고 dev 덤프 재생성으로 일괄 이관한다** | `00_base_schema.sql`(신규 설치는 이미 `learning_diagnosis_*`로 생성되어 43이 no-op — 43은 00 갱신 이전에 구축된 **기존 DB**의 `free_diagnosis_*` 3종을 rename하기 위한 것), `20`/`30`/`34`의 시드 필요 |
| 44 | `44_special_highschool.sql` | 특목고합격 페이지 신규 스키마: `special_highschool_cases`(합격 사례 카드, school_type CHECK 자사고/외고/영재고/과학고, UNIQUE 없음 — 동명이인 마스킹 충돌 허용, 사례 38건 시드는 테이블이 비어있을 때만) / `special_highschool_acceptance_rates`(연도별 합격률, 컬럼명이 `admission_acceptance_rates`와 동일해 공용 프론트·어드민 컴포넌트가 그대로 재사용, year UNIQUE + 2021~2025 5개년 시드 `on conflict do nothing`) 신규 테이블 2종 + 인덱스 5종 + public read/`is_winning_admin()` admin write 정책. 히어로 대학 로고는 신규 테이블 없이 기존 `admission_case_logos`를 그대로 공유한다 | `00_base_schema.sql`의 `is_winning_admin()` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 45 | `45_faq_renewal.sql` | FAQ 페이지 리뉴얼: `public.faqs`에 `category text not null default ''`(한글 표시값 그대로 저장, 슬러그·CHECK 금지 — 라벨은 운영팀 원본 문서(`위닝에듀_FAQ.docx`, 2026-08-06 수령) 기준 7개로 확정: 서비스 소개 / 회원가입 및 이용환경 / 서비스 이용 방법 / 개인정보 및 데이터 보안 / 요금제 및 결제 / 학교·기관 도입 (B2G) / 기타 문의. CHECK를 걸지 않는 이유는 라벨 미확정이 아니라 카테고리가 운영팀이 언제든 바꾸는 편집 가능한 카피이고, 입력 통제는 어드민 select가 담당하기 때문) + `content_json jsonb`(교육칼럼과 동일한 BlockNote 블록 이중 저장, `answer` text는 평문 미러로 유지) 컬럼 추가 + 공개 페이지 목록 쿼리(`is_active, sort_order, created_at desc`)를 미러링하는 복합 인덱스 1종 + FAQ 30건 시드(위 7개 카테고리 전량, `(category, question)` 조합 `where not exists` 멱등 가드). RLS 정책 무변경(기존 `faqs_public_read`/`faqs_admin_all`이 신규 컬럼을 자동 커버) | `00_base_schema.sql`의 `faqs` 필요. 다른 마이그레이션과 독립 실행 가능. **프런트 배포 전 반드시 선행 실행** — 컬럼 없이 배포하면 어드민 저장이 PGRST204로 실패 |
| 46 | `46_profiles_role_hardening.sql` | `profiles.role` 권한상승(privilege escalation) 긴급 패치: 기존 `profiles_update_own` 정책의 WITH CHECK가 "내 행인가"만 검사하고 role 값을 고정하지 않아, 임의의 일반 회원이 브라우저 콘솔에서 `supabase.from('profiles').update({ role: 'admin' })` 한 줄로 관리자가 될 수 있었다(2026-08-06 dev DB 실측 확인). 방어선도 전무했다 — `profiles_role_check`는 `role in ('user','admin')`이라 'admin'을 애초에 허용하고, 컬럼 레벨 GRANT/REVOKE와 BEFORE UPDATE 가드 트리거는 `sql/` 전체에 0건이며, `is_winning_admin()`이 `profiles.role`을 그대로 신뢰하므로 이 컬럼이 뚫리면 그 함수가 지키는 모든 write 정책이 함께 뚫린다. 조치는 `profiles_update_own` 하나만 drop/재생성해 WITH CHECK에 `role = (select p.role from public.profiles p where p.id = auth.uid())`(= role 값 불변) 조건을 추가하는 것 — 이름·연락처 등 일반 컬럼 수정은 계속 동작한다. 어드민 경로는 무손상: 별도 PERMISSIVE 정책 `profiles_admin_update_all`(`using/with check (is_admin())`)을 이 파일이 건드리지 않고, 같은 커맨드의 PERMISSIVE 정책은 OR로 결합되므로 어드민은 role을 포함해 계속 자유롭게 UPDATE할 수 있다. 대안이던 `revoke update (role) ... from authenticated`는 **기각** — 컬럼 권한이 RLS보다 먼저 평가돼 어드민(DB 레벨에선 동일한 authenticated 롤)까지 막히고, `Admin.jsx`의 제네릭 저장 경로가 이름만 고쳐도 role 키를 함께 실어 보내 회원 관리 저장 전체가 `permission denied for column role`로 죽는다. 파일 말미에 임시 `auth.users`+`profiles` 행으로 셀프 승격을 실제 시도해보는 검증 `do` 블록이 포함돼 있으며, **`P0001: SELFTEST esc_sqlstate=42501 ... role_after=user own_rows=1` 에러로 끝나는 것이 정상**이다(`raise exception`이 트랜잭션을 되돌려 임시 행을 자동 정리). *(이 행은 dev에 파일만 머지되고 표 갱신이 누락된 것을 이 브랜치에서 보충한 것)* | `00_base_schema.sql`의 `profiles`(role 컬럼)·`profiles_update_own`/`profiles_admin_update_all` 정책·`is_admin()` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 50 | `50_board_renewal.sql` | 게시판(공지사항·회사소식) 리뉴얼: `company_news`/`notices`에 `view_count integer not null default 0` 추가(`galleries` 선례와 동일 타입) + 조회수 1일 1회 중복 방지 대장 `board_views`(PK 4-튜플 `(source, post_id, viewer_key, viewed_on)`, RLS on **정책 0건** + anon/authenticated 테이블 권한 revoke — 오직 아래 RPC 경유로만 기록. SECURITY DEFINER 함수가 소유자 권한으로 RLS를 면제받는 조합이므로 `force row level security`를 켜면 안 된다. `viewer_key`는 `md5(IP\|UA\|일자\|source\|post_id)` 해시로 **원본 IP를 저장하지 않는다** — pgcrypto 가용 여부를 확인할 수단이 없고 런타임 분기 시 확장 설치 전/후로 키가 갈려 중복 방지가 조용히 깨지므로 내장 `md5()` 확정) + 조회수 증가 RPC `increment_board_view(p_source text, p_id uuid) returns integer`(**p_source 화이트리스트 필수** — 없으면 anon이 임의 테이블 UPDATE 가능. IP는 pooler를 거쳐 `inet_client_addr()`가 무용하므로 `request.headers` GUC의 `x-forwarded-for` 첫 항목 사용. 일자 경계 KST. `revoke all from public` → `grant execute to anon, authenticated`. service_role에는 우리가 명시적으로 grant하지 않았으나, Supabase가 public 스키마 함수에 기본 부여하는 EXECUTE가 `revoke ... from public`으로 회수되지 않아 실측상 service_role에도 EXECUTE가 붙어 있다 — 이미 RLS 우회 권한을 가진 롤이라 무해하므로 그대로 둔다) + `view_count`만 바뀐 UPDATE에서 `updated_at`을 원값으로 되돌리는 가드 트리거 2종(`zz_*_keep_updated_at` — 알파벳 순으로 기존 `set_company_news_updated_at`/`trg_notices_updated_at` **뒤**에 실행되도록 이름을 잡았다. 기존 트리거는 drop·재정의하지 않고 추가만 하므로 `00_base_schema.sql` 재실행에도 살아남는다) + `notices_display_idx`(기존 `idx_notices_events_visible`은 전부 ASC라 실제 정렬 `is_pinned desc, sort_order, created_at desc`와 어긋남 — base 재실행 시 되살아나므로 drop 없이 추가만) | `00_base_schema.sql`의 `company_news`/`notices`(+ 두 테이블의 `updated_at` 트리거). 다른 마이그레이션과 독립 실행 가능. 번호 갭(46~49)은 전부 선점 때문이다 — 46번은 dev에 머지된 `46_profiles_role_hardening.sql`(`profiles` RLS만 건드림), 47~49번은 아직 미머지인 `admission-structured-json` 브랜치가 커밋 상태로 점유(`47_admission_section_json.sql` / `48_admission_resource_index_json_flags.sql` / `49_app_settings.sql`). 46~49 중 어느 것도 이 파일의 `company_news`/`notices`/`board_views`/`increment_board_view`와 겹치는 객체가 없어 **번호만 뒤일 뿐 실제 선행 의존은 아니며**, 43~49를 건너뛴 DB에서도 단독 실행된다. 47~49가 미머지 상태라 머지 순서에 따라 이 파일의 번호가 또 바뀔 수 있다(이 파일이 먼저 머지되면 저쪽 3개가 밀린다). **프런트 배포 전 반드시 선행 실행** — 컬럼 없이 배포하면 어드민 저장이 PGRST204로 실패 |
| 51 | `51_faq_copy_update.sql` | FAQ 카피 갱신(2026-08-07 운영팀 원고): 카테고리 라벨 `학교·기관 도입 (B2G)` → `학교 및 기관 도입` rename(해당 3행) + 답변 6행 문구 교정. **전체 재삽입이 아니라 부분 UPDATE** — 새 원고를 45번 시드/현 dev 30행과 기계 대조한 결과 질문 30개는 순서·문구까지 100% 동일하고 답변도 30개 중 29개가 동일해, delete+insert 하면 얻는 것 없이 id(uuid)만 새로 발급되며 어드민 편집 이력·정렬 기준이 흔들린다(`id`/`sort_order`/`is_active` 무변경, 행 삭제·재삽입 없음). 답변 변경은 내용 변경 1건(환불 신청 경로 `온라인신청` → `마이페이지 내 수강/결제내역`) + 사용자가 "명백한 오타만" 으로 확정한 교정 5건(`구성 됩니다`→`구성됩니다` / `로그인 후␣␣목표관리서비스` 이중공백 / `무료 진단서비스은`→`는` / `관룐 자료`→`관련 자료` / `2026년 9월␣␣정식` 이중공백). `answer`(평문 미러)와 `content_json`(정본 블록 봉투)을 같은 `replace()` 로 함께 갱신하되 봉투는 `jsonb_set(content_json, '{blocks,0,content,0,text}', …)` 로 text 하나만 교체한다 — dev 30행 실측상 blocks 길이 1/`type='paragraph'`/content 길이 1 이고 `blocks[0].content[0].text = answer` 가 30/30 일치라, 이 경로면 블록 UUID·`editor`(`blocknote@0.52.1`)·props 가 전부 보존되고 **새 UUID 를 지어낼 필요가 없다**(`jsonb_set` 은 strict 라 `content_json` null 인 행은 null 그대로 남아 오염되지 않는다). 멱등성은 각 UPDATE 의 `where` 에 옛 문구 조건을 넣어 확보 — 재실행은 조용히 0행이 되므로 실제 반영 여부는 파일 하단 검증 SELECT(카테고리 분포·옛 문구 잔존 0건·`content_json` ↔ `answer` 동기 30건·봉투 보존)로 판정한다. `updated_at` 은 기존 트리거에 맡기고 직접 대입하지 않는다. 시제(`2026년 9월 정식 오픈 하였습니다`)와 `고객센터(이메일/전화번호 입력 예정)` 플레이스홀더는 사용자 지시로 **보류**(이 파일 범위 밖) | `45_faq_renewal.sql`의 `category`/`content_json` 컬럼 + FAQ 30건 시드 필요(그 시드가 없으면 전 UPDATE 가 0행). **프런트 배포 전 반드시 선행 실행** — 컬럼 추가가 없어 PGRST204 류 저장 실패는 없지만, `src/data/faqCategories.js` 만 새 라벨로 배포되면 옛 라벨로 저장된 3행이 어떤 카테고리 탭에도 안 잡히고 '전체' 탭에만 노출되는 **조용한 카피 누락**이 난다(`src/pages/Faq.jsx:62` `visibleTabs` 가 `FAQ_CATEGORIES` 와의 교집합만 탭으로 그린다) |
<!-- 43/45/46은 origin/dev에 다른 마이그레이션(43_admission_results.sql/
     45_faq_renewal.sql/46_profiles_role_hardening.sql)이 먼저 그 번호를
     썼다(2026-08-06 확인). 이 브랜치의 원래 43/45/46은 47/48/49로
     재번호했다 — 파일명만 바뀌었고 dev DB 적용 상태는 그대로다. -->
| 47 | `47_admission_section_json.sql`(구 `43_admission_section_json.sql`) | 대입모집요강 구조화 문서(AdmissionDoc) jsonb 컬럼 추가: `admission_university_resources`에 `previous_year_changes_json`/`selection_method_json`/`minimum_requirements_json`/`exam_schedule_json`/`school_record_method_json`/`recruitment_quota_json` 6종(전부 `<rawKey>_json` 접미어로 통일 — html 쪽 `recruitment_result_html` 비대칭을 재생산하지 않음) + `jsonb_typeof(col)='object'` 형태 제약 1종. `*_html`은 미러로 유지, 삭제하지 않음. 백필 정본은 `scripts/import-legacy-admission-html.mjs`(저장 HTML을 구조화 doc으로 재구성, 1253/1253셀 성공), `scripts/backfill-admission-doc.mjs`는 임포터 실패 셀의 rawHtml 폴백 전용으로 역할이 좁혀졌다. **dev 적용 완료(2026-08-06)** — 218행 전체 백필 완료(`source` 분포: `legacy-html` 1242 / `bundled-special` 11, `blocks` 길이 0인 행 0건) | `00_base_schema.sql`의 `admission_university_resources`, `38_admission_universities.sql`의 admin/public RLS 정책(테이블 단위 상속, 재정의 없음) 필요 |
| 48 | `48_admission_resource_index_json_flags.sql`(구 `45_admission_resource_index_json_flags.sql`) | `39_admission_resource_index_view.sql`의 목록 뷰에 `has_*_json` 6종 존재 여부 플래그 추가(끝에 append — grant 보존). `47_admission_section_json.sql`의 형태 제약이 `jsonb_typeof(col)='object'` 수준으로만 가벼워 `blocks` 배열 형태를 보장하지 않으므로, `jsonb_typeof(col->'blocks')='array'`를 CASE로 먼저 확인한 뒤에만 `jsonb_array_length`를 호출해 뷰 select 전체가 throw하는 것을 막는다. 본문 jsonb는 뷰에 노출하지 않는다(39번 신설 사유인 statement timeout 재발 방지). `security_invoker = true` 유지. **dev 적용 완료(2026-08-06)** — 백필 후 `has_*_json` true 카운트가 `*_json is not null` 카운트와 정확히 일치 확인 | `39_admission_resource_index_view.sql`(뷰 자체), `47_admission_section_json.sql`(대상 컬럼) 필요 |
| 49 | `49_app_settings.sql`(구 `46_app_settings.sql`) | 전역 앱 설정 키-값 테이블 `app_settings(key text primary key, value jsonb, updated_at)` 신설 + public read(anon,authenticated)/`is_winning_admin()` admin write 정책 + `admission_active_year` 시드(기본 2027, `on conflict do nothing`). 대입모집요강 "연도별 관리" 도입으로 `AdmissionGuidelines.jsx`의 `ACTIVE_ADMISSION_YEAR` 하드코딩을 이 값으로 대체(공개 노출 연도는 관리자가 명시적으로 지정 — 최신 자동 노출 아님). `page_contents`/`admission_universities`에 얹지 않고 전용 테이블로 분리했다(도메인 불일치 방지, 향후 다른 전역 설정 확장 대비). ⚠ public read가 전부 true이므로 비밀값(키·토큰)을 이 테이블에 넣지 말 것 | `00_base_schema.sql`의 `is_winning_admin()` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 50 | `50_admission_resource_index_official_url.sql` | 목록 뷰 `admission_university_resource_index`에 `official_source_url`(대학 입시 홈페이지 링크) 1컬럼 추가(끝에 append — grant 보존, `create or replace view`). 공개 목록의 대학명 셀을 이 URL로 가는 `<a target="_blank" rel="noopener noreferrer">`로 승격하기 위한 선행 조건 — 컬럼이 뷰에 없으면 `row.official_source_url`이 `undefined`라 전 대학이 **조용히** 평문으로 렌더된다(콘솔 에러·게이트 실패 없음). `jungsi_guideline_url`('정시모집요강 보기' 버튼용)과는 별개 컬럼이며 이 파일은 그쪽을 건드리지 않는다. 본문 raw/html/jsonb는 계속 뷰에 노출하지 않는다(39번 statement timeout 재발 방지). `security_invoker = true` 유지 | `48_admission_resource_index_json_flags.sql`(뷰 직전 정의), `00_base_schema.sql`의 `admission_university_resources.official_source_url` 필요 |
| 52 | `52_mentor_applications.sql` | 이용신청 > 멘토신청(`/mentor-apply`) 데이터 레이어: 멘토 지원서 테이블 `mentor_applications`(지원자정보/대학·전형/출신고교/멘토역량/증빙·동의 5블록, 셀렉트 3종 CHECK — `enrollment_status` 재학/휴학/졸업, `weekly_capacity` 1~2회/3~5회/6~9회/10회 이상, `available_timeslot` 평일 오후/평일 저녁/주말 오전/주말 오후/주말 저녁, `request_ip inet` — `api/mentor-apply.js`의 IP rate limit 조회용, `proof_file_name text` — 클라이언트가 올린 원본 파일명 기록용(경로는 서버가 UUID로 생성하므로 원본명이 경로에 남지 않음, 표시 시 이스케이프 필요), 이미 실행된 DB를 위해 두 컬럼 모두 `alter table ... add column if not exists`도 병행) + 어드민 목록·상태·IP 인덱스 3종 + `updated_at` 트리거(공용 `set_updated_at()` 재사용) + **비공개** Storage 버킷 `mentor-applications`(`public = false`) + `file_size_limit`(50MB = 52428800 bytes) · `allowed_mime_types`(PDF/PNG/JPG + HWP 통용 MIME 3종 `application/x-hwp`·`application/haansofthwp`·`application/vnd.hancom.hwp`, 컬럼 존재 미확정이라 실패해도 버킷 생성엔 영향 없도록 별도 update 문으로 분리) — **첨부 업로드가 signed upload URL 방식(클라이언트 → Storage 직접 업로드)으로 바뀌며 Vercel Functions 4.5MB body 상한을 우회, Storage 계층 제약이 주 방어선이 됨. Supabase 프로젝트 전역 업로드 상한을 Management API로 실측 완료 — dev(`gjowqdiopinhixfivnkx`)·운영(`ucjlcvqvinspmrasvsug`) 모두 `fileSizeLimit: 52428800`(50MB)로 동일하며, 이는 SQL로 제어 불가한 값이라 버킷 상한을 여기에 맞춤(시안 원문 100MB에서 2026-08-10 사용자 승인 하에 50MB로 변경, 100MB가 필요해지면 요금제 확인 + 전역 상한 상향 선행 필요)** + `storage.objects` 어드민 select 정책 + **`public.phone_verifications.purpose` CHECK 확장**(`'mentor_apply'` 추가 — `api/send-phone-code.js`의 `ALLOWED_PURPOSES`에는 이미 있으나 40번 파일의 CHECK가 막고 있어, 알림톡 발송 이후 단계인 `phone_verifications` insert가 23514로 실패해 본인인증이 영구히 완료 불가능했던 문제를 해소). **지원자는 비회원**이라 클라이언트 insert/select 정책을 하나도 두지 않는다 — RLS enable + 정책 미부여 = 전면 거부이고, 제출은 service_role을 쓰는 `api/mentor-apply.js` 한 경로만(휴대폰 인증 재확인·파일 MIME/크기 검증·경로 서버 생성·rate limit이 그 라우트에 있다). 단 서버 전용 3종(`phone_verifications` 등)과 달리 `revoke ... from anon, authenticated`는 **걸지 않는다** — 테이블 권한이 RLS보다 먼저 평가돼 DB 레벨에서 같은 authenticated 롤인 **어드민 조회까지 막히기** 때문(46번이 기록한 컬럼 GRANT 기각 사유와 동일). `user_id`는 nullable이지만 비회원 제출이라 항상 null이라 인덱스를 만들지 않는다. 증빙 파일은 개인정보라 버킷이 비공개이므로 어드민 열람은 `createSignedUrl(path, TTL)` — `Admin.jsx`의 `getPublicUrl` 관용구(public 버킷 전용)를 그대로 쓰면 안 된다 | `00_base_schema.sql`의 `is_winning_admin()`·`set_updated_at()`, `auth.users`. `40_auth_signup.sql`의 `public.phone_verifications` 테이블 — purpose CHECK 확장이 이 파일에 있어 40번이 먼저 실행돼 있어야 한다. (1)~(5)는 이와 별개로 다른 마이그레이션과 독립 실행 가능. **프런트/`api/mentor-apply.js`/`api/send-phone-code.js` 배포 전 반드시 선행 실행** — 테이블·버킷 없이 배포하면 제출이 전량 실패하고, purpose CHECK 미확장 상태로는 휴대폰 인증 자체가 완료될 수 없다 |
| 52 | `52_learning_diagnosis_path_move.sql` | 학습진단 URL 6종 통일 규칙 이관: 43번이 만든 소개 페이지 경로 `/learning-diagnosis`(운영 미적용 DB는 여전히 `/free-diagnosis`)를 확정 규칙(`/services/{slug}`)에 맞춰 `/services/learning-diagnosis`로 갱신. `program_categories.link`/`page_contents.slug`/`banners.button_link` 3컬럼만 갱신하고 라벨(`학습진단`)은 43에서 이미 정본이라 손대지 않는다. where 절에 구 값 2종(`/free-diagnosis`, `/learning-diagnosis`)을 모두 넣어 43 적용 여부와 무관하게 한 문장으로 신 경로에 수렴한다(2단계 이관을 1단계로 흡수). 앱(설문) 경로 `/app/learning-diagnosis/survey`는 DB 어디에도 저장돼 있지 않아(전수 확인 완료, 프런트 하드코딩) 이 파일 갱신 대상이 아니다. `page_contents.slug`는 `/`로 시작하는 절대경로라 `resolveMenuLink()`가 `/page/:slug`(DynamicPage) 대신 그대로 내비 링크로 쓰는 행이라 라우팅 영향 없음(같은 패턴이 `slug='/faq'` 행에도 이미 존재) | `43_learning_diagnosis_rename.sql`(선행 없이도 실행 가능 — where 절이 43 미적용 상태의 `/free-diagnosis`도 함께 매칭) |
| 53 | `53_mentor_apply_faq_admin.sql` | 멘토신청(`/mentor-apply`) §7 FAQ 어드민 전환: 신규 `mentor_apply_faqs`(질문/답변/정렬/노출, `(is_active, sort_order, created_at)` 인덱스) + `mentor_apply_copy`(반복되지 않는 단문 카피 키-값, `copy_key`/`copy_value`, 이번엔 FAQ 헤더 3키만 시드) 테이블 2종 + public read(anon,authenticated)/`is_winning_admin()` admin write 정책 + `updated_at` 트리거(공용 `set_updated_at()` 재사용) + `src/data/mentorApply.js`의 `MENTOR_FAQ`/`FAQ_SECTION` 원문을 그대로 옮긴 시드(마커 `53_mentor_apply_faq_admin_seed_v1`로 최초 1회만 — 이후 어드민 편집값 보존). 미적용 시 공개 페이지는 번들 상수로 폴백해 현재와 동일하게 동작하지만, 어드민 화면은 테이블이 없으면 조회·저장이 실패하므로 **어드민 배포 전 반드시 선행 실행** | `00_base_schema.sql`의 `is_winning_admin()`·`set_updated_at()` 필요. 다른 마이그레이션과 독립 실행 가능 |
| 54 | `54_admission_results_2yr.sql` | 입결정보 4개년 → **2개년(2025·2026)** 전환 + 마스터 xlsx(`입결_마스터_2개년.xlsx`, 43,170행×17열) 수용: **`recruitment_period` 축 제거**(원본 3종 어디에도 모집시기 개념이 없어 값이 항상 '수시' 하나뿐 — `admission_results`는 이로써 **수시 전용 테이블**이 된다. 뷰 2종 `drop view` → 유일성·셀렉터 인덱스 drop → CHECK drop → `drop column` 순서 필수, `create or replace view`로는 컬럼 축소가 불가하다 `cannot drop columns from view`) + 신규 지표 컬럼 7종(`grade_85`/`grade_90`은 43에 이미 있고 구버전 DB 대비 `if not exists` 병행, `grade_avg`/`grade_min`/`grade_avg10`/`grade_min10`/`grade_first_avg` 신규 — 전부 커버리지 1% 미만이라 **적재만 하고 v1 화면 미노출**) + 분할모집 변별자 `variant_seq smallint NN default 0`(자연키로도 안 갈라지는 진성 중복 28그룹/29행 — 정원내·정원외, 단계별 분할모집 추정) + **유일성 인덱스 재정의**(`result_year, university_key, department_key, coalesce(main_track,''), coalesce(screening_category,''), admission_track, coalesce(subject_reflection,''), variant_seq` — 기존 키에 `main_track`이 없어 그대로 적재하면 중심전형(교과 vs 종합)만 다른 **983행이 조용히 손실**된다) + 값 도메인 CHECK 2종(`main_track` 교과/종합/논술/실기/기타 — xlsx 원문이 접두어 없는 표기라 `학생부교과`가 아니라 `교과`로 저장하고 표시 레이어가 어차피 `/^학생부/`를 지우므로 화면은 동일 / `screening_category` 11종 + 기타) + 어드민 인덱스 2종(`result_year desc, id desc` 정렬 + `pg_trgm` GIN 검색 — 434행 시절과 달리 43k행에서는 필수) + sql/43 누락분 `trending_departments` `updated_at` 트리거 보정. **⚠️ 완전한 idempotent가 아니다** — `delete from admission_results`가 매 실행 전량을 지운다. 백업은 고정 이름이 아니라 `admission_results_backup_YYYYMMDD_HH24MISS`로 **실행 시각마다 새 테이블**을 떠서(RLS enable, 정책 없음 = 전면 차단) "2회차엔 백업이 갱신되지 않아 적재분이 무백업 삭제되는" 함정을 해소했지만, **최초 1회 실행이 원칙**이고 검증 끝난 백업은 수동 drop할 것. dev 434행(더미 시드, 슬러그 키 체계라 신규 한글 정규화 키와 혼재 불가)은 파기 승인됨. 적재 자체는 이 파일에 없다 — 로더가 CSV를 만든 뒤 스테이징 테이블 경유로 별도 실행한다. `trending_departments` 큐레이션 행은 키 체계 전환으로 딥링크가 전부 깨지므로 **적재 후 재큐레이션 필요**(이 파일 범위 밖) | `43_admission_results.sql` 필수 선행(`admission_results`/`trending_departments`/뷰 2종이 있어야 한다). **운영 DB에는 `admission_results` 테이블 자체가 없으므로 43 → 54 순서로 실행** |
| 74 | `74_goal_schedules.sql` | 목표관리 중요일정(수행평가/시험/제출마감/기타) 데이터 레이어: 신규 `goal_schedules`(학생당 다건, `profile_id → goal_students(profile_id) on delete cascade`, `category` CHECK 4종(performance/exam/deadline/etc), `title` 트림 길이 CHECK 1~100자, `due_date`, `memo`) + `(profile_id, due_date)` 인덱스(목록·대시보드 레일 공유 정렬 축) + RLS(select own + admin **select**(`goal_schedules_admin_select`, for select만 — `55_goal_management.sql`의 `_admin_all`(for all)을 그대로 베끼면 어드민 세션에 쓰기가 열려 하우스 규칙 위반이라 select 전용으로 좁혔다, `59_refund_request_hardening.sql`/`64_entitlement_period_sessions.sql`의 `_admin_select_all` 전례와 동일 패턴), **클라이언트 write 정책 0건** — 쓰기는 service_role인 `api/goal/schedules.js` 단일 경로) + `updated_at` 트리거(공용 `set_updated_at()` 재사용). D-day는 저장하지 않고 클라이언트가 오늘(KST) 기준 매번 계산한다. **파일만 존재 — dev DB 미적용**(팀장 수동 실행 대상) | `00_base_schema.sql`의 `set_updated_at()`, `55_goal_management.sql`의 `goal_students`(FK 대상) 필요 |
| 55 | `55_goal_management.sql` | 목표관리 서비스(`/app/goal`) 데이터 레이어. 외부 원본 앱(`target/api/student.mjs`)의 역산 산물이라 **모든 컬럼에 원본 출처(`student.mjs:줄`)를 `comment on column`으로 남겼다** — 출처 없이는 6개월 뒤 고칠 수 없는 스키마다. 테이블 4종: `goal_students`(사용자당 1행, PK가 `profile_id uuid → auth.users(id)` — 원본 물리 키 `code`(무인증 IDOR의 근원, `student.mjs:2533` `onConflict:'code'`)와 `main_id`를 둘 다 폐기했다. base 확률 4종 + rate 4종 + `study_schedule` jsonb + `status`(`awaiting_cuts`/`active`/`paused`)) / `goal_daily_records`(가상 날짜 — `record_index`(0-base)가 정본 충돌키이고 `record_date`는 파생, `virtual_day_index`는 `record_date`의 generated column. 확률 증분 `delta_*` 4종은 **서버 전용**) / `goal_probability_logs`(확률 추이 스냅샷, append-only, `reason`+`source_record_id` 신설) / `goal_university_cuts`(컷 기준표 — 원본 `universities`가 `admission_universities`(38번)와 충돌하고 실제 내용도 "대학 목록"이 아니라 컷 표라 개명. **`avg_cut` 스케일 이원성에 CHECK 필수** — `normal`/`special`은 내신 등급 1~9(작을수록 우세), `jungsi`는 정시 백분위 0~100(클수록 우세)로 섞이면 확률 계산의 우열 판정이 통째로 반전된다. 원본엔 제약이 전혀 없었다) + 현재확률 뷰 `goal_student_state`(`security_invoker = true`). **현재확률을 컬럼으로 캐시하지 않는 것이 이 파일의 핵심 결정** — 원본이 클램프를 "합계"에 걸기 때문에(`student.mjs:1034-1037`) 증분을 누적 저장하면 값이 갈린다(base 95, +10, -10 → 재계산 95 vs 캐시 90). 즉 캐시는 성능 최적화가 아니라 **동작 변경**이고 203개 테스트로 동결된 `src/lib/goal/calc/`와의 파리티가 깨진다. ⚠ 뷰는 `least()/greatest()`가 NULL을 **무시**하는 PostgreSQL 동작(`greatest(0, NULL) = 0`) 때문에 `case when base is null then null` 가드를 반드시 거친다 — 없으면 `awaiting_cuts` 학생이 "미산출"이 아니라 "확률 0%"로 보여 이 뷰의 존재 이유가 깨진다. RLS는 4테이블 전부 enable + 정책 8종이되 **클라이언트 write 정책이 0건**이다 — `base_*`/`rate_*`/`delta_*`가 확률 게이지 그 자체이고 RLS는 행 단위라 컬럼을 가릴 수 없어, update를 열면 학생이 자기 확률을 100으로 쓸 수 있다(원본은 실제로 브라우저에서 증분을 계산해 서버가 그대로 저장했다, `App.tsx:1620-1631` → `student.mjs:2662-2667`). 쓰기는 service_role을 쓰는 `api/goal/*.js` 단일 경로. `revoke ... from anon, authenticated`는 걸지 않는다(52번·46번의 기각 사유 — 테이블 권한이 RLS보다 먼저 평가돼 어드민 조회까지 막힌다). `goal_university_cuts`만 `is_active = true` public read(anon 포함 — 원천 `admission_results`가 이미 public read다). `updated_at` 트리거 3종은 공용 `set_updated_at()` 재사용(`goal_probability_logs`는 append-only라 없음). 컷 데이터 백필은 이 파일 범위 밖 | `00_base_schema.sql`의 `is_winning_admin()`(1338행)·`set_updated_at()`(1432행), `auth.users`. 다른 마이그레이션과 독립 실행 가능. **`api/goal/*.js` 배포 전 반드시 선행 실행** — 테이블 없이 배포하면 온보딩 저장이 전량 실패한다 |
| 76 | `76_goal_workbooks.sql` | 목표관리 "나의 노력" 화면(과목별 문제집 진도) 데이터 레이어: `goal_workbooks`(사용자당 다건, `subject` CHECK는 `subjectTokens.js` 5종 id — korean/math/english/science/etc, `total_pages`/`current_page`/`status`(reading/done) — 완독 판정은 `api/goal/workbooks.js`가 계산해 저장, DB CHECK로 두 컬럼을 교차 검증하지 않음) + `(profile_id, subject)` 인덱스 + RLS(55번:718-731 관례 — select own/admin, 쓰기 정책 0건) + `updated_at` 트리거(공용 `set_updated_at()` 재사용) | `55_goal_management.sql`의 `goal_students(profile_id)`(FK 대상), `00_base_schema.sql`의 `is_winning_admin()`·`set_updated_at()`. **`api/goal/workbooks.js` 배포 전 반드시 선행 실행**. dev DB 미적용(파일만) |
| 53 | `53_performance.sql` | 수행평가 인앱 이식 선행 인프라(로드맵 P1 ①②③) — `winning_assessment_knowledge_items` 지식베이스 교정 3종. **(1) RLS 교정**: `00_base_schema.sql:2109-2112`의 `for ALL to authenticated using(true) with check(true)` 전면 개방 정책을 폐기하고 `winning_assessment_knowledge_admin_all`(`using/with check (is_admin())`) 하나로 교체. **정책 제거는 이름을 적어 지우지 않고 `pg_policies`를 훑어 이 테이블의 정책을 전부 drop** 하는 do 블록으로 한다 — PERMISSIVE 정책은 **OR 결합**이라 이름이 다른 개방 정책이 하나라도 남으면 `is_admin()` 잠금이 무효인데 `drop policy if exists`는 대상이 없으면 조용한 no-op이라 마이그레이션이 **에러 없이 성공**하며 뚫린 사실을 은폐한다. 정책 이름의 근거가 dev 재생성 덤프 하나뿐인데 이 repo는 dev↔운영 drift를 전제로 운용하므로(아래 「운영 DB 반영」 관례) 운영에 과거 다른 이름의 정책이 남아 있을 여지가 실재한다. 최종 상태가 정책 정확히 1개라 drop-all 후 재생성해도 멱등이며, 파일 말미 `(1-c)` 검증 do 블록이 정책 수가 1이 아니면 `raise exception`으로 터뜨려 조용한 성공을 막는다. (함수 오버로드를 oid로 훑는 `(3-a)`와 같은 방어다.) — 이웃 테이블 `winning_base_data`/`winning_db_inputs`는 이미 `is_admin()`으로 잠겨 있는데 이 테이블만 예외였고, 수행평가 인앱 전환으로 **학생 계정이 같은 Supabase에 authenticated로 붙는 순간** 유료 상품의 원천 자산인 지식베이스가 읽기·수정·삭제에 전면 개방된다. 판정 함수는 README 「RLS admin 판정」의 랜딩 관례(`is_winning_admin()`)가 아니라 **`is_admin()`**을 쓴다 — ㄱ) `is_winning_admin()`이 추가로 인정하는 role 3종(`admin_basic`/`admin_manager`/`admin_master`)은 `profiles_role_check`(`:815`, `role in ('user','admin')`) 때문에 이 DB에 **존재할 수 없어** 유효 판정 대상이 동일하고, ㄴ) `is_winning_admin()`은 uid 외에 **JWT email 문자열**로도 profiles 행을 찾는데 `profiles.email`에 UNIQUE가 없어 판정 축만 넓어진다. README가 `is_winning_admin()`을 못박은 사유(42P17 recursion)는 두 함수 모두 SECURITY DEFINER라 동일하게 해소하므로 둘을 가르지 못한다. 명세서 로드맵 P1 ⑤도 같은 판정을 명시. **읽기는 "전면 차단"이 아니라 "어드민만"**: `src/pages/Admin.jsx`의 위닝DB 3개 섹션(`:2094`/`:2159`/`:2236`)이 서버 라우트를 경유하지 않고 **브라우저 supabase 클라이언트로 직접** select(`:6115`)/insert(`:6259-6273`)/update(`:6332`)/delete(`:6305`)하므로 select를 통째로 막으면 어드민 화면이 죽는다. 서버 RAG 경로는 service_role이라 RLS를 우회해 정책 없이도 읽힌다. `revoke ... from anon, authenticated`는 **걸지 않는다**(46·52번이 기록한 함정 — 테이블 권한이 RLS보다 먼저 평가돼 어드민 조회까지 막힌다). **(2) `source_link text` 컬럼 신설 + 백필**: 외부 앱 `getRowSourceLink`가 `source_link`/`url`/`link` 등 10키 탐색 + `meta`/`metadata`/`extra` JSON 파싱을 하지만 목적지에 그 컬럼이 하나도 없어 전부 no-op이고 실효 경로는 `row.source` 정규식 1줄뿐이었다(= 컬럼 부재로 생긴 우회). 기존 행은 외부 앱과 **동일 판정**(`/https?:\/\/[^\s)]+/i` → `substring(source from '(?i)(https?://[^\s)]+)')`)으로 백필하며, 값이 이미 있는 행은 건드리지 않아 재실행 안전(마커 불필요, updated_at 트리거 없음). 링크 없는 행이 빈 채 남는 것은 정상. **(3) RPC `match_winning_suhaeng_all_subjects` 재생성**: 반환에 `source_link text` 추가(similarity 앞 append) + `filter_subject text default null` 파라미터 맨 뒤 추가. 기존 5개 파라미터의 이름·타입·순서·기본값(`match_threshold` 0.52 포함)과 `filter_grade` '공통'/'전체'/'확인 필요' 폴백·`knowledge_type in ('topic_pattern','verified_resource')` 제한·`limit least(match_count,20)`은 **그대로**라 이름 기반 5키로 호출하는 기존 호출부는 무손상. RETURNS TABLE 변경이라 `create or replace`가 42P13으로 거부되므로 DROP 후 재생성이며, 구 5인자 오버로드가 남으면 PostgREST 호출이 42725(function is not unique)로 깨지므로 **do 블록이 같은 이름의 모든 오버로드를 oid(`regprocedure`)로 훑어 drop**한다(인자 타입을 손으로 적는 방식은 `vector`가 extensions 스키마라 search_path에 좌우된다). `filter_subject`의 술어는 **`filter_grade`와 같은 모양**이다(`filter_subject is null or w.subject = filter_subject or w.subject in ('공통','전체','확인 필요')`) — 명세서 §8(`:1862`) 지시이자 레거시 키워드 경로의 `subjectCandidates`(외부 앱 `api/_lib/dynamic-knowledge.js:282-289`)와도 일치한다. 정확 일치로 좁히면 subject가 '공통'/'전체'로 태깅된 행이 벡터 경로에서 **에러 없이** 빠져 리콜만 줄고 키워드 폴백으로 떨어진다(명세서 §8:1853이 지목하는 실패 양식). HNSW 인덱스(`:1101`)는 opclass·부분 인덱스 술어가 RPC와 일치해 **무변경**, subject 인덱스 추가도 실측 전까지 보류 | `00_base_schema.sql`의 `winning_assessment_knowledge_items`·`is_admin()`·`match_winning_suhaeng_all_subjects`·`extensions.vector` 필요. 다른 마이그레이션과 독립 실행 가능. **수행평가 이식 코드 배포 전 반드시 선행 실행** — 컬럼·반환 컬럼 없이 새 코드가 뜨면 `source_link`가 조용히 비고, `filter_subject`를 넘기는 순간 RPC 호출이 PGRST202로 전량 실패한다. **배포 런북 — 이 SQL 실행 후 force 백필이 필수다**: `POST /api/performance/admin-embed { "action":"backfill", "force":true }`(관리자 Bearer, 1회 최대 50건)를 응답의 `embedded`가 0이 될 때까지 반복한다. 사유: `search_text` 빌더의 `출처 링크` 줄이 기존 코퍼스와 달라진다 — 외부 앱에서는 링크 컬럼이 없어 항상 빈 문자열이었으나 (2)의 백필 이후 실제 URL이 들어간다. 재임베딩을 건너뛰면 한 테이블에 서로 다른 공식의 벡터가 섞여 `match_threshold`(0.48~0.52) 대비 코사인 유사도가 **에러 없이** 흔들리고, 저장할 때마다 정확 일치 캐시가 미스가 나 Gemini 비용이 반복된다. force는 API 플래그로만 존재하고 어드민 UI 버튼이 없으므로 이 줄이 유일한 실행 근거다 |
| 54 | `54_performance_app.sql` | 수행평가 인앱 앱 스키마(로드맵 P2) — 학생 산출물 층 신설. 53번이 **어드민 지식베이스**를 잠갔다면 이 파일은 **학생 산출물**을 담는다(명세서 §8.1 「학생 산출물 저장 테이블은 전무하다」). **(1) 8테이블**: `performance_sessions`(외부 `api_sessions`+`conversations` 통합 대체 — 외부는 main_id당 1행 upsert라 회원당 진행 세션이 구조적으로 1건이었고 두 번째를 시작하면 첫 번째가 덮였다), `performance_messages`(채팅 타임라인, `unique(session_id, seq)`), `performance_attachments`(안내문 이미지 — 분석 API는 storage path가 아니라 **이 행의 id만** 받는다, 외부 IDOR 차단), `performance_topics`(라운드당 3장, `detail`은 6요소 고정 배열 — 외부 프롬프트는 7항목이라 시안에 없는 섹션이 렌더됐다), `performance_reports`(design/evaluation/final_submission 3종 — 외부의 `draft` 리포트 행은 **이식하지 않는다**, 중간저장은 submissions.is_draft), `performance_submissions`(fields 자유 키 jsonb — 초판의 4필드 고정은 오류, 문항형 최대 20필드), `performance_credit_ledger`, `performance_session_vectors`(초판이 통째로 누락한 학생 과거 수행 RAG 층). **결정 3종(이식 금지)**: ㄱ) `sessions.school_type`에 리터럴 기본값 `'일반고'`를 **넣지 않는다** — 외부 `api/login.js:65` 하드코딩을 이식하면 프로필이 빈 계정의 프롬프트에 사실이 아닌 값이 **에러 없이** 주입되고 리포트에 그대로 남는다. 값 없으면 `null`, 프롬프트는 `미입력`. 이 컬럼은 `profiles.school_type`의 **세션 시점 스냅샷**이라 가입 후 프로필을 바꿔도 과거 세션 입력이 흔들리지 않는다. ㄴ) 학년·학기는 STEP1 폼 입력값이 정본이며 `grade_label`/`semester`로 **분리 저장**(외부는 `고1 1학기` 결합 문자열). ㄷ) `selected_topic = '주제|||상세'` 결합 저장과 오타 컬럼 `selected_topic_detai`는 이식하지 않고 `selected_topic_id uuid` FK로 정규화. 소유자 식별은 `profiles.id`(= `auth.uid()`) 하나이며 외부 text `students.code`/`main_id` 축은 폐기(FK·RLS를 걸 수 없고 동명이인 충돌). **(2) RLS 소유자 전용**: 8테이블 전부 RLS on + **소유자 SELECT만**. INSERT·UPDATE·DELETE 정책은 8테이블 어디에도 만들지 않는다 — §8.6 엔드포인트 표를 실측하면 **클라이언트 직접 write 경로가 0건**이고 전부 service_role 서버리스를 경유한다(읽기만 클라이언트 supabase-js 직접 select가 정본). 초안에 있던 `sessions`/`submissions` 소유자 INSERT·UPDATE는 **철회**했다: RLS는 행 단위라 컬럼을 가릴 수 없어서, ㄱ) 소유자 INSERT가 열려 있으면 `api/performance/session.js`의 409 `UNCHARGED_SESSION_EXISTS` 하나로만 강제되는 **미차감 세션 1개 제한**을 supabase-js 직접 insert로 우회해 회차 차감 없이 Gemini vision(analyze-guide)을 무한 호출할 수 있고, ㄴ) 소유자 UPDATE는 '클라이언트가 위조할 수 있었다 → 서버 소유로 승격'이 도입 사유인 `guide_json`·`submission_schema`(및 `status`/`completed_steps`)의 위조 경로를 되살린다. 철회로 잃는 기능은 없다(실제 write가 전부 service_role이다). 세션 하위 6테이블은 SECURITY DEFINER 헬퍼 `public.performance_owns_session(uuid)`로 소유권을 판정한다 — 정책 본문에 `exists(select ... from performance_sessions)`를 직접 쓰면 하위 1행마다 **상위 테이블 RLS가 재평가**되고 정의가 6곳에 복제된다(하나만 빠뜨려도 조용히 뚫린다). 헬퍼는 PK 등가 조회 1건이고 `auth.uid()` 고정 기준이라 남의 세션 id를 넣어도 false만 나온다. `performance_credit_ledger`는 **write 정책을 하나도 만들지 않는다** — 차감은 서버 RPC만 한다. 어드민 정책은 만들지 않았다(performance_*를 직접 조회하는 어드민 화면이 아직 없고 서버 경로는 service_role이라 RLS 우회 — 화면이 생기면 53번과 같이 `is_admin()`으로 추가). `revoke ... from anon, authenticated`는 **테이블에는 걸지 않는다**(46·52·53번이 반복 기록한 함정). **(3) RPC ① `consume_performance_credit(p_session_id uuid, p_profile_id uuid, p_reason text default 'recommend-topics:first-success') → jsonb`**: 회차 차감 단일 트랜잭션. **차감 지점은 주제 추천 최초 성공 1곳뿐**이고 외부 앱의 4지점 선차감(`recommend-topics:72`/`analyze-assessment-storage:60`/`evaluate-text:51`/`find-resources:331`)은 버그라 이식 금지(§9.3). 멱등 가드는 `performance_credit_ledger.session_id` **UNIQUE**로, "다른 주제 다시 추천은 무차감"이 코드 규율이 아니라 **스키마 제약**으로 보장된다. 회차는 `program_access.meta.{quota_total,quota_used}`(§9.3 2항·§9.4 2단계가 지정한 위치, 신규 컬럼을 만들지 않는다)이며 `program_key`는 운영 값 `'suhaeng'` 고정(신규 자산은 performance 네이밍이지만 이 값은 운영 DB·`serviceAccess.js`·랜딩 `serviceKey`에 박혀 있어 바꾸면 결제-이용권 연결이 끊어진다). 이용권 행은 `serviceAccess.js:checkProgramAccessTable`과 같은 컬럼 우선순위(`id`→`profile_id`→`user_id`)로 1행만 잡고 **`for update`로 잠근다** — 잠그지 않으면 두 탭이 같은 quota_used를 읽어 한 번이 공짜가 된다(외부 `incrementCallCount`의 read-then-write 결함). ⚠ **sentinel이 외부 앱과 반전**돼 있다: 외부는 `limit === 0`을 무제한으로 읽는데(`api/_lib/sessions.js:229`) 신규 SSO 학생이 정확히 `call_limit: 0`으로 생성돼(`api/login.js:222-223`) 그대로 이식했다면 **첫날부터 전 신규 사용자가 무제한**이 됐을 것이다. 목적지는 **키 부재**=무제한 / **`0`=소진** / `>0`=잔여(`quota_total - quota_used`)다. 값이 같아도 의미가 정반대라 어떤 에러도 나지 않으므로 외부 코드를 참고해 고칠 때 반드시 파일 `(4-a)` 주석을 먼저 볼 것. **키 부재와 오염(정수 문자열이 아닌 값)은 가른다** — 키 부재만 무제한 폴백(P15 전 임시, `raise warning`으로 관측)이고 오염은 `0`=소진(fail-closed)이다. 데이터 손상이 조용히 무제한으로 승격되지 않게 하기 위함이다. 원장 조회는 **`for update` 잠금 이후**에 한다 — 순서를 뒤집으면 같은 세션 동시 요청 2건(더블클릭·재시도)에서 뒤늦은 쪽이 갱신된 `quota_used`로 소진을 판정해 **이미 차감이 끝난 자기 세션**에 409 QUOTA_EXHAUSTED를 돌려준다(§9.3 「이미 차감된 세션은 계속 진행 허용」 위반). 또 잠근 행의 `access_status`(expired/suspended)·`payment_status`(refunded/cancelled)·`coalesce(access_expires_at, expires_at)` 만료를 확인해 명세에 없는 5번째 status **`entitlement_expired`**(→403)로 가른다 — 보지 않으면 환불·정지·기간 만료된 행으로도 차감이 성립해 §2.2가 외부 앱 결함으로 지목한 동작이 그대로 재현된다. 이 검사는 멱등 반환보다 **뒤**라서 이미 차감된 세션은 만료 후에도 계속 진행된다. 행 선택 `order by`도 유효한 행을 선두에 두어 `serviceAccess.js:findProgramAccessRow`(statusRank 추가)와 같은 행을 잡는다. **(4) RPC ② `match_student_performance_sessions(query_embedding vector, filter_profile_id uuid, match_count int default 8, match_threshold float default 0.48)`**: 학생 과거 수행 유사도 검색. 외부 `match_student_reports_all_subjects`의 재설계본으로 threshold **0.48**과 프롬프트 렌더러가 실제로 읽는 반환 필드를 승계하되, text `student_code` 필터를 uuid `profile_id`로 대체하고 **소유자 격리 3중 방어**를 넣는다 — ㄱ) `filter_profile_id is null`이면 즉시 예외(외부는 studentCode가 비면 조용히 전체 폴백으로 떨어졌다), ㄴ) `auth.uid()`가 있는데 인자와 다르면 예외(service_role은 uid가 null이라 통과), ㄷ) where 절 소유자 일치 + SECURITY INVOKER라 authenticated 호출에는 RLS가 한 번 더 걸린다. `rag_use` 필터는 `coalesce(rag_use,true)`가 아니라 **`= true`** 다 — 이 컬럼은 옵트인(`not null default false`)이고 "평가 리포트 생성/최종 확정 시에만 승격"이 규정이라 coalesce 폴백을 쓰면 규정이 무력화된다(외부는 draft도 rag_use=true라 미완성 초안이 과거 실적으로 프롬프트에 들어갔다). `consume_performance_credit`은 **EXECUTE를 회수하고 service_role에만 부여**한다(53번의 "revoke 금지"는 테이블 권한 이야기이고 함수 EXECUTE는 별개). HNSW 인덱스는 `00_base_schema.sql:1101` 패턴 준용(`vector_cosine_ops` + `where embedding is not null`). **(5) Storage**: 버킷 `performance-guides` **private**(`public=false`) + 10MB/`image/png,image/jpeg,image/webp`(HEIC 거부) + **소유자 경로 select 정책만**(insert/update 정책은 55번에서 철회 — 서명 업로드 토큰은 service_role 발급이라 RLS를 타지 않으므로 정상 경로에 불필요하고, 두면 이용권 없는 로그인 계정이 supabase-js로 직접 올려 5장/25MB 상한과 `performance_attachments` 행을 통째로 우회한 채 cleanup에도 안 잡히는 영구 잔존물을 만든다). 경로는 `{profile_id}/{session_id}/{uuid}.{ext}`로 외부의 `purpose` 세그먼트를 제거했다(프론트가 절대 호출하지 않는 데드 분기). delete 정책은 만들지 않는다 — 원본 삭제는 90일 cron·분석 후 정리(둘 다 service_role)만 한다. `file_size_limit`/`allowed_mime_types` update는 52번과 같이 **별도 문**으로 분리(컬럼 부재 42703이 버킷 생성을 깨지 않게). storage.objects 정책은 SQL Editor postgres 롤로 42501이 날 수 있으며 그 경우 대시보드에서 동일 정의로 수동 생성. **명세와 다르게 구현한 8가지는 파일 `(7)` 절에 표로 정리**했다(career_goal NOT NULL → status 조건부 CHECK / raise 대신 status jsonb 반환 / RLS 축소 / 보정 행 vs UNIQUE 충돌 / rag_use 필터 / vectors에 created_at·updated_at 추가 / `entitlement_expired` status 추가 / 어드민 정책 생략). **검증**: pgvector/pgvector:pg16 컨테이너에 base 스텁을 얹고 전량 실행해 2회 연속 무에러(멱등) + 회차 차감 케이스(무이용권·비소유·최초차감·재호출 멱등·소진·sentinel 키부재=무제한/0=소진/오염=소진 fail-closed·환불/정지/기간만료=entitlement_expired·만료 후에도 기차감 세션은 already_charged·stale 행과 유효 행 공존 시 유효 행 선택·동시 2커넥션 더블클릭에서 뒤늦은 쪽이 quota_exhausted가 아니라 already_charged) + 벡터 검색 소유자 격리·threshold·null 가드 + RLS 소유자 격리·ledger write 차단·profile_id 재배정 차단·delete 0행 + `is_final` 부분 UNIQUE를 실측 통과시켰다 | `00_base_schema.sql`의 `profiles`·`program_access`·`set_updated_at()`·`extensions.vector` 필요. `53_performance.sql`과는 **독립**(참조 객체가 겹치지 않는다)이나 실행은 번호 순서대로. **수행평가 앱 코드(`api/performance/*`, `/app/performance`) 배포 전 반드시 선행 실행** — 테이블 없이 새 코드가 뜨면 PGRST205로 전 엔드포인트가 죽는다. 🚧 **배포 게이트 — P15 없이 프로덕션에 올리지 않는다**: `api/confirm-payment.js`에 `program_access` 문자열이 0건이라 `meta.quota_total`을 채우는 주체가 아직 없다(§9.4 BLOCK, P15). 그래서 RPC의 sentinel 폴백을 "키 없음 = 무제한"으로 뒀다 — 아니면 회차를 받을 방법이 없는 상태에서 전 사용자가 즉시 소진 판정을 받는다. **이 폴백은 fail-open이라 이 상태로 배포하면 이용권 보유자 전원이 무제한이 되어 '3개월 6회' 상품이 에러 하나 없이 무력화된다**(외부 앱 `call_limit:0`=무제한 결함과 같은 실패 형태, 방향만 반대). 폴백을 탈 때마다 `raise warning`이 남으니 서버 로그로 관측할 것. 순서는 P15(products ALTER + confirm-payment 회차 부여) → 기존 이용자 meta 백필 → 폴백을 fail-closed로 뒤집기 → 프로덕션 배포다(파일 `(8)-ㄱ`). 또한 `admin_enrollments` 경로 이용자는 `program_access` 행이 없어 이 RPC가 `no_entitlement`를 돌려준다(명세 §9.3·§9.4를 따른 결과) — 기존 enrollment 이용자가 있으면 P15에서 `program_access` 백필이 필요하다. **미차감 세션 동시 1개 제한**(§9.3 무료 vision 게이트 차단)은 두 테이블에 걸친 조건이라 부분 UNIQUE로 표현할 수 없어 여기서 강제하지 않는다 — `api/performance/session.js`가 `409 UNCHARGED_SESSION_EXISTS`로 낸다. 90일 보관 cron(`api/performance/cleanup-attachments.js` + `vercel.json` `crons`)도 아직 없고 인덱스만 미리 깔아 뒀다. **stale `program_access` 행**도 P15 착수 전 확인 대상이다(파일 `(8)-ㅁ`) — 진입 게이트 `serviceAccess.js:checkProgramAccessTable`은 기간 만료를 아예 보지 않고(만료 배치도 없다, §11-Q56) `isPaidStatus`/`isActiveStatus`가 부분 문자열 매칭이라 `'unpaid'`·`'inactive'`까지 통과시킨다. 그래서 RPC의 유효성 검사는 허용 목록이 아니라 거부 목록(환불·정지·만료)으로 뒀다 — 여기만 조이면 게이트는 통과했는데 차감에서만 막히는 사용자가 생긴다. 두 계층을 함께 조이는 것은 P15 작업이다 |
| 55 | `55_performance_guide_hardening.sql` | 수행평가 안내문 첨부 하드닝(P7 검토 지적 3건). **(1) `storage.objects`의 `performance guides owner insert`/`owner update` 정책 철회** — 두 정책은 `to authenticated` + 첫 경로 세그먼트=`auth.uid()`뿐이라 **이용권 없는 로그인 계정도** supabase-js로 자기 uid 하위에 직접 업로드할 수 있었고, 그 경로에는 세션당 5장·합계 25MB 상한도 `performance_attachments` 행도 적용되지 않는다. cleanup 잡(`api/performance/cleanup-attachments.js`)은 그 테이블 행만 순회하므로 **DB 행 없는 객체는 90일 cron·24시간 스윕 어느 쪽에도 안 잡혀 영구 잔존**한다 — 업로드 카드의 `90일 후 자동 삭제` 고지가 그 경로에서만 거짓이 되고 스토리지가 무한히 는다. 업로드는 `upload-url.js`가 발급한 signed upload URL로만 이뤄지고 그 토큰은 service_role이 만들어 **RLS를 타지 않으므로** 정상 경로는 그대로 동작한다(같은 방식인 `mentor-applications` 버킷도 insert 정책이 없다, 52번 (4)). select 정책은 남기고 delete는 54번과 같이 만들지 않는다. 54번 파일에서도 두 정책 생성문을 제거했으므로 신규 DB(54만 실행)와 기존 DB(54→55)가 같은 상태로 수렴한다. **(2) `performance_attachments.cleanup_attempts int not null default 0` + `cleanup_last_error_at timestamptz`** — cleanup 잡의 head-of-line blocking 해소용. 조회가 `deleted_at is null` + `created_at` 오름차순 + `limit(batch+1)`이고 remove 실패분은 (의도적으로) `deleted_at`을 안 찍으므로, 영구적 이유로 계속 실패하는 오래된 묶음이 배치 앞자리를 영원히 점유해 뒤의 만기분이 상한 안에 못 들어온다(90일 약속이 조용히 멈추는데 `hasMore`는 계속 true라 정상 적체와 구분 불가). 잡이 실패 시 카운터를 올리고 조회에서 `cleanup_attempts < 5`로 걸러 실패분이 배치를 막지 않게 하며, 임계 초과분은 응답 `stuck`으로 노출한다. 인덱스는 추가하지 않는다(54번 두 부분 인덱스가 선두 조건을 담당, `cleanup_attempts`는 잔여 필터). **(3) `performance_sessions.guide_analysis_count int not null default 0`** — `analyze-guide`는 §9.2에 따라 **무차감**이라 회차가 남용 억제 수단이 못 되고, 계정 하나로 5장짜리 vision 호출(최대 11k maxOutputTokens·최대 25MB 다운로드·429/503 시 3회 시도)을 무제한 반복시킬 수 있었다. 차감을 늘리는 대신(§9.2 위반) **세션당 호출 횟수 상한**을 이 컬럼으로 건다. 서버(service_role)만 갱신하며, 멱등 단축 반환(요청 첨부가 전부 `done`이고 `guide_json.mode='upload'`)은 카운터를 올리지 않는다 | `54_performance_app.sql` 필요(같은 테이블·버킷을 전제한다). 전부 `drop policy if exists` / `add column if not exists`라 여러 번 실행해도 안전. storage.objects 정책 drop이 SQL Editor postgres 롤로 42501이 나면 대시보드 Storage > Policies에서 같은 이름 두 정책을 수동 삭제할 것(54·52·31번이 반복 기록한 함정) |
| 56 | `56_performance_topic_hardening.sql` | 수행평가 주제 추천 하드닝(P8 검토 지적). **`performance_sessions.topic_attempt_count int not null default 0`** — `recommend-topics`의 구조 실패(422 `MODEL_CONTRACT_VIOLATION` / 503 `MODEL_UNAVAILABLE`)는 §8.4에 따라 **무차감**이고 `performance_topics`에도 행을 남기지 않는데, 라운드 상한(`MAX_ROUNDS=3`)은 저장된 행의 `round` 최댓값으로만 계산되므로 **실패 경로에서는 영원히 오르지 않는다.** 스키마 준수를 방해하도록 조작한 입력(예: `guide_freetext`)으로 같은 세션에서 무한 반복 호출이 가능하고 1회당 임베딩 2회 + 최대 6회(과부하 재시도 3 × 구조 재시도 2)의 Gemini 생성 호출이 나간다. 55번 (3)의 `guide_analysis_count`와 같은 방식으로 **세션당 모델 시도 횟수 상한**(코드 상수 10)을 이 컬럼으로 건다 — 차감을 늘려 막지 않는다(§9.2 「과금과 남용 방지를 분리한다」). **두 축은 다르다**: `MAX_ROUNDS`는 성공 라운드 수(사용자에게 보이는 상한, `409 ROUND_LIMIT`), 이 컬럼은 성공·실패를 모두 세는 시도 수(`429 TOPIC_ATTEMPT_LIMIT`). 서버(service_role)만 갱신하고 **모델을 실제로 부르기 직전**에 오르며, 저장된 라운드를 그대로 돌려주는 멱등 재생은 올리지 않는다. 회차 차감과 무관하므로 `consume_performance_credit`은 건드리지 않는다 | `54_performance_app.sql` 필요. `add column if not exists`라 여러 번 실행해도 안전. **`api/performance/recommend-topics.js` 배포 전 반드시 선행 실행** — 이 라우트가 컬럼을 select 목록에 넣으므로 없으면 PostgREST 42703으로 라우트 전체가 죽는다(55번 `guide_analysis_count`와 같은 선행 조건) |
| 57 | `57_performance_design_report.sql` | 수행평가 설계 리포트(로드맵 P10) — **주제 확정 + 리포트 생성을 한 트랜잭션으로 묶는 층**. §8.6이 이 엔드포인트를 「주제 확정 + 리포트 생성을 한 트랜잭션으로(외부는 2회 왕복이고 앞이 실패해도 뒤가 진행됨)」로 규정한다. 외부 앱은 `save-topic-selection` → `find-resources` 2회 왕복이었고 프론트가 앞의 실패를 무시하고 뒤를 불러 "주제는 확정 안 됐는데 리포트만 있는" 세션과 그 반대가 둘 다 생겼다. **(1) `performance_reports.topic_id uuid`**(설계 리포트 대상 주제, `on delete set null` — 리포트는 스냅샷이라 주제 행이 지워져도 남긴다. evaluation/final_submission은 null이라 NOT NULL을 걸지 않는다) **+ `updated_at timestamptz not null default now()`**(설계 리포트는 세션당 1행을 덮어쓰며 재생성하므로 created_at만으로는 재생성 시점을 알 수 없다. 트리거는 걸지 않고 아래 RPC에서 명시적으로 찍는다 — write 주체가 service_role 서버리스뿐이라 갱신 지점이 하나다). **(2) 부분 UNIQUE 인덱스 `performance_reports_one_design_per_session_idx on (session_id) where report_type='design'`** — §8.6이 저장 리포트 조회용으로 규정한 집계 뷰 `v_performance_saved_reports(..., design_report_id, ...)`가 이미 "세션당 설계 리포트 1건"을 조회 계약으로 못박고 있다. 그래서 재생성은 새 행을 쌓지 않고 **같은 행을 덮어쓴다**(아래 RPC의 `on conflict (session_id) where report_type='design'` 충돌 대상이며, 동시 요청 2건이 두 행을 만드는 것도 함께 막는다). `performance_submissions_one_final_per_session_idx`(54번 1-6)와 같은 수법 — 부분 UNIQUE는 테이블 제약으로 표현할 수 없어 인덱스로 만든다. ⚠ 이미 세션당 design 행이 2개 이상인 DB에서는 인덱스 생성이 실패한다(신규 기능이라 그런 행은 존재할 수 없다). **(3) `performance_sessions.design_generation_count` / `design_attempt_count` (둘 다 `int not null default 0`)** — 56번이 주제 추천에서 확립한 **2축 분리**를 그대로 따른다. `design_generation_count`는 **성공한** 생성만 세며 §9.3 「설계 리포트 생성·재생성 | 없음 (재생성 상한 2회)」 = 최초 1 + 재생성 2 = 3이 상한이고 초과 시 §8.6의 `429 RATE_LIMITED{limit:2}`다. `design_attempt_count`는 **모델 호출 시도**를 성공·실패 모두 세는 별도 축이다 — 구조 실패(422/503)는 무차감이고 리포트 행도 남기지 않아 생성 카운터가 오르지 않으므로, 그 축만으로는 실패만 반복하는 경로에 상한이 없다(1회당 임베딩 2회 + 최대 6회의 Gemini 생성 호출). 55번 `guide_analysis_count`·56번 `topic_attempt_count`와 완전히 같은 구멍이고 같은 방식으로 막는다 — **차감을 늘려 막지 않는다**(§9.2, 애초에 설계 리포트는 무차감 단계라 회차가 억제 수단이 될 수도 없다). attempt는 모델 호출 직전 핸들러가 +1, generation은 RPC 안에서 리포트 저장과 같은 트랜잭션으로 +1, 멱등 재생(replay)은 둘 다 올리지 않는다. **(4) RPC `commit_performance_design_report(p_session_id uuid, p_profile_id uuid, p_topic_id uuid, p_sections jsonb, p_model text default null, p_prompt_version text default null) → jsonb`** — 사용자 가시 write 4종(`performance_topics.selected` 재설정 / `sessions.selected_topic_id`·`status`·`current_step`·`completed_steps` / `design_generation_count` / `performance_reports` upsert)을 **단일 트랜잭션**으로 묶는다. supabase-js에 다중 문장 트랜잭션이 없어 핸들러에서 update를 순서대로 쏘면 반쪽 상태가 남기 때문에 커밋 구간 전체를 plpgsql로 내렸다. 주제 확정은 `set selected = (id = p_topic_id)`로 **세션 전체를 한 문장에 다시 칠한다**(이전 선택 해제를 별도 문장으로 두면 그 사이에서 죽었을 때 선택 2건인 세션이 생긴다). `completed_steps`에 **3만** 넣는다 — STEP3(주제 추천)은 추천을 받은 순간이 아니라 사용자가 확정하는 순간 끝나고(§5.11, `recommend-topics.js:stepPatch` 주석이 "그 확정은 design-report가 기록한다"고 예고), STEP4는 리포트가 만들어진 순간이 아니라 사용자가 모달을 닫고 작성 단계로 넘어가는 순간 끝난다(§5.13 `창 닫고 작성하기`). `current_step`은 `greatest`라 이미 STEP5까지 간 세션을 재생성이 되돌리지 않는다. `security definer`라 소유권을 함수 안에서 직접 확인하고, 54번 관례대로 raise 대신 status jsonb를 돌려준다(`committed` / `session_not_found`→403 / `topic_not_in_session`→404). ⚠ **이 함수는 회차를 건드리지 않는다** — 설계 리포트는 무차감이고(§9.3) 차감 지점은 `recommend-topics` 최초 성공 1곳뿐이라 `performance_credit_ledger`도 `program_access.meta`도 읽지도 쓰지도 않는다. EXECUTE는 회수하고 **service_role에만** 부여한다 — RLS를 우회하는 write 진입점이라 authenticated가 직접 부를 수 있으면 자기 세션에 임의 `sections`를 밀어 넣어 리포트를 위조할 수 있다(53번의 "revoke 금지"는 **테이블** 권한 이야기이고 함수 EXECUTE는 별개, 54번 `consume_performance_credit`과 같은 처리). **검증**: postgres:16 컨테이너에 54번 3테이블 스텁을 얹고 전량 2회 연속 무에러(멱등) + 최초 커밋(주제 1건만 selected·`completed_steps {1,2}`→`{1,2,3}`·`current_step 3`→`4`·`status draft`→`in_progress`) / 재생성(design 행 1개 유지·`report_id` 불변·`sections` 교체·`updated_at` 갱신·generation 2) / 남의 세션→`session_not_found` / 타 세션 주제→`topic_not_in_session` / `current_step=5`에서 재커밋해도 5 유지 / 부분 UNIQUE가 evaluation 2행은 허용 / EXECUTE는 service_role만 t를 실측 통과 | `54_performance_app.sql` 필요(`performance_reports`·`performance_topics`·`performance_sessions`를 전제한다). 55·56과는 독립이나 실행은 번호 순서대로. **`api/performance/design-report.js` 배포 전 반드시 선행 실행** — 이 라우트가 (3)의 두 컬럼을 select 목록에 넣고 (4)의 RPC를 호출한다. 컬럼이 없으면 PostgREST 42703으로 라우트 전체가 죽고, 함수가 없으면 **모델을 다 부른 뒤 마지막 커밋에서** 42883으로 터져 사용자는 로딩만 오래 보고 500을 받는다(55번 `guide_analysis_count`·56번 `topic_attempt_count`와 같은 선행 조건) |
| 58 | `58_performance_submission.sql` | 수행평가 STEP5 제출·평가·확정(로드맵 P11) — **제출물 커밋 층**. **(1) `performance_reports.submission_id uuid`**(평가·최종 리포트가 대상으로 삼은 제출본, `on delete set null`. design은 null이라 NOT NULL을 걸지 않는다). 57번 `topic_id`와 같은 이유다 — 리포트 정체성을 세션의 현재 상태로 유도하면 **재제출 직후 화면이 이전 평가 리포트를 새 제출본의 결과로 보여준다.** 이 컬럼 덕분에 멱등 재생 판정(`submission_id`가 같으면 모델 미호출)과 `finalize`의 평가 선행 조건(§8.6 `400 NO_EVALUATION_YET`)이 조인 없이 성립한다. **(2) 부분 UNIQUE 2개** `performance_reports_one_evaluation_per_session_idx` / `..._one_final_per_session_idx` — 57번 (2)가 `design`에 건 것과 같은 근거(§8.6 집계 뷰 `v_performance_saved_reports`가 세션당 리포트 id를 종류별로 하나씩 들고 있다). 그래서 **재평가는 새 행을 쌓지 않고 같은 행을 덮어쓴다**(외부는 재평가마다 `assessment_reports` 행이 쌓이고 프론트가 '첫 등장 선점'으로 골랐다 — §12.2가 버그로 지목한 동작). 대가로 이전 평가 리포트 본문은 남지 않지만 제출본 자체는 revision별로 전부 남는다. **(3) `performance_sessions.evaluation_count` / `evaluation_attempt_count`(둘 다 `int not null default 0`)** — 56·57번이 확립한 **2축 분리**. `evaluation_count`는 성공한 평가만 세며 §9.2 상한 표 「재평가 | 2회 | `409 REEVALUATION_LIMIT`」 = 최초 1 + 재평가 2 = 3이 상한이고, `evaluation_attempt_count`는 성공·실패를 모두 세는 모델 호출 축이다(모델 실패 502는 리포트를 남기지 않아 앞 축이 오르지 않으므로 실패만 반복하는 경로에 상한이 없다). **차감을 늘려 막지 않는다**(§9.2) — 이 단계는 애초에 무차감이라(§9.3 「제출 → 평가 리포트 생성 | 없음」) 회차가 억제 수단이 될 수도 없다. attempt는 모델 호출 직전 핸들러가 +1, evaluation은 RPC 안에서 리포트 저장과 같은 트랜잭션으로 +1, 멱등 재생은 둘 다 올리지 않는다. **(4) RPC `commit_performance_evaluation_report(uuid,uuid,uuid,jsonb,smallint,text,text,text) → jsonb`** — 제출 확정(`is_draft=false`/`submitted_at` 최초 1회만) + 평가 리포트 upsert(세션당 1행) + 진행 단계(`completed_steps ∪ {4}`, `current_step` greatest 5) + `evaluation_count` +1 + **`performance_session_vectors.rag_use` 승격**을 단일 트랜잭션으로 묶는다. `is_final`은 건드리지 않는다(최종본 고정은 (5) 몫). **(5) RPC `finalize_performance_submission(uuid,uuid,uuid,text,jsonb) → jsonb`** — `is_final` 고정 + `final_submission` 리포트 upsert + 세션 `completed` + `rag_use` 승격. 외부 `api/finalize-submission.js:46-72`는 조건 없는 insert라 재시도·다중 탭에 중복 행이 쌓였다 → 여기서는 ㄱ) `performance_submissions_one_final_per_session_idx`(54번 1-6) 부분 UNIQUE, ㄴ) 같은 제출본 재확정은 **아무것도 바꾸지 않고** `already_final`(§8.6 「이미 확정된 세션이면 동일 200(멱등)」 — `finalized_at`·`finalize_reason`을 덮어쓰지 않는다), ㄷ) 다른 제출본이 이미 확정이면 `already_finalized_other`(→409, Q67 「확정 후에도 재평가를 허용하되 최종본은 1건만 고정」의 뒷부분)로 세 겹이다. 대상 행을 `for update`로 잠가 동시 2건에서 뒤늦은 쪽이 23505(500)가 아니라 ㄴ·ㄷ으로 정상 분기한다. `finalize_reason`은 외부 `action` 값 승계(`confirm`/`new_assessment`)이며 §12.2 정본대로 **두 값 모두 최종본을 확정 저장**한다('추가 평가 받기'만 확정 없이 폼을 복원하므로 이 RPC를 부르지 않는다). ⚠ `status='completed'`는 **세션 잠금이 아니다** — 재방문 재개 후보에서 빠질 뿐이고 `submission.js`·`evaluate.js` 어느 쪽도 `status`를 게이트로 쓰지 않는다(Q67). **`rag_use` 승격은 정확히 2지점**(평가 커밋 / 최종 확정)이다 — §8.3 「평가 리포트 생성 또는 최종 제출 확정 시에만 true 승격. 외부는 draft도 `rag_use=true`라 미완성 초안이 과거 실적으로 프롬프트에 들어갔다(`api/_lib/reports.js:279`)」. ⚠ **벡터 행 자체는 만들지 않는다**(행 생성·임베딩 큐는 P14 몫) — 지금은 update가 0행에 적중하지만, 나중에 끼워 넣으면 그 사이 세션이 영원히 옵트인되지 않으므로 승격 지점만 먼저 박아 둔다. ⚠ **이 파일은 회차를 건드리지 않는다** — `performance_credit_ledger`도 `program_access`도 읽지도 쓰지도 않는다(차감 지점은 `recommend-topics` 최초 성공 1곳뿐, §9.3). 외부 `api/evaluate-text.js:51`의 선차감은 §12.4 폐기 대상이다. 두 RPC 모두 `security definer` + 소유권 함수 내 직접 확인 + raise 대신 status jsonb 반환(54번 (7) 관례)이고, **EXECUTE는 회수하고 service_role에만 부여**한다(authenticated가 직접 부르면 임의 `sections`·`score`를 밀어 넣어 리포트를 위조하고 상한을 우회할 수 있다). **검증**: `scripts/verify-performance-submission-api.mjs`(부분 UNIQUE 3종·rag_use 2지점·`for update`·멱등 분기 3종·EXECUTE 회수·라우트 select 컬럼 실재) | `54_performance_app.sql` 필요(`performance_reports`·`performance_submissions`·`performance_sessions`·`performance_session_vectors`를 전제한다) + `57_performance_design_report.sql` 필요(이 파일의 두 RPC가 57번이 만든 `performance_reports.updated_at`을 갱신한다). **`api/performance/evaluate.js`·`finalize.js`·`submission.js` 배포 전 반드시 선행 실행** — `evaluate.js`가 (3)의 두 컬럼을 select 목록에 넣으므로 없으면 PostgREST 42703으로 라우트가 죽고, 함수가 없으면 **모델을 다 부른 뒤 마지막 커밋에서** 42883으로 터져 사용자는 로딩만 오래 보고 500을 받는다(55·56·57번과 같은 선행 조건) |
| 72 | `72_performance_saved_reports_view.sql` | 수행평가 저장 리포트 집계 뷰(로드맵 P12, §8.6:1836): `v_performance_saved_reports`(`security_invoker = true`) — `performance_sessions` ← `performance_topics`(주제명) ← LATERAL `performance_reports`(design/evaluation/final_submission 3종의 `report_type`별 최대 1행, 58번 부분 UNIQUE 전제) ← LATERAL `performance_submissions`(최근 갱신 시각 집계) 조인. `design_report_id`/`evaluation_report_id`/`final_report_id` 셋 다 `performance_reports.id`를 가리킨다 — `final_report_id`는 §8.6 문면(`performance_submissions.is_final`)과 달리 `finalize_performance_submission`(58번 (5))이 같은 트랜잭션에서 만드는 `report_type='final_submission'` 행을 가리키게 했다(그 행의 `sections`만 `SectionedReportView`가 그대로 소비할 수 있는 봉투다). `updated_at`은 세션·리포트·제출본 갱신 시각의 `greatest()`(NULL 무시라 s.updated_at 하나로도 안전) | `54_performance_app.sql` 필요, `57_performance_design_report.sql`(`performance_reports.updated_at`) + `58_performance_submission.sql`(부분 UNIQUE 3종) 필요. `api/performance/reports.js` 배포 전 반드시 선행 실행 — 뷰 없이 배포하면 PGRST205로 라우트가 죽는다 |

> 52 중복: mentor_applications(dev) / learning_diagnosis_path_move(goal-app-shell) — 실행 순서 주의

> 53 건너뜀: 미머지 브랜치 `performance-app`이 `53_performance.sql`로 선점하고 있어 목표관리는 54로 피했다. 접두어가 곧 수동 실행 순서라 중복 번호는 실제 실행 사고를 낸다. `performance-app`이 끝내 머지되지 않으면 53은 빈 번호로 남는다(갭 자체는 무해 — 46~49 선례).

> `partner_universities`(학교리스트 섹션) 테이블·RLS·시드는 2026-07-27 최종본에서 섹션 자체가 삭제되어 이 파일에서 제거됨. 이미 실행된 운영 DB에 테이블이 남아있다면 별도 `drop table` 정리는 필요 시 수동 진행.

> 43번(학습진단 리네이밍)은 `00_base_schema.sql`의 `free_diagnosis_*` 객체명도 함께 신 이름으로 갱신했다 — 00은 동결 스냅샷이 원칙이나, 빈 DB 신규 설치가 구 이름 테이블을 만든 뒤 43이 rename하는 이중 상태를 피하기 위한 예외다.

> ⚠️ **54번(`54_performance_app.sql`)의 `consume_performance_credit` 정의는 구버전(program_access.meta 기반)이다.** `checkout-renewal` 브랜치(아직 push 전)의 `sql/64~66`이 같은 함수를 부여 원장 `program_access_grants` 기반으로 재작성해 dev DB에 이미 배포했다. 이 저장소는 마이그레이션 러너가 없어 파일 번호가 곧 수동 실행 순서이므로, 54번을 재실행하면 64~66의 신버전을 덮어쓴다 — 다만 조용히 meta 회계로 되돌아가지는 않는다. checkout-renewal이 `performance_credit_ledger.grant_id`를 NOT NULL로 추가했는데 구버전 본문은 그 컬럼을 채우지 않으므로, **첫 차감 시도에서 즉시 23502(not-null violation)로 큰소리로 실패한다.** 54번을 재실행했다면 반드시 64~66을 다시 적용해라.

> ⚠️ **번호 54가 두 브랜치에서 각자 다른 파일로 충돌한다.** 이 브랜치(`performance-app`)는 `sql/54_performance_app.sql`을, `checkout-renewal` 브랜치는 `sql/54_program_access_grant.sql`을 같은 번호로 쓴다. 후자는 `programs` 시드 파일로 `program_key` 정본을 `goal` → `target`으로 정정할 뿐이며 `program_access_grants`/`performance_credit_ledger`는 만들지 않는다(`create table` 없음, 두 테이블명 참조 없음) — **둘은 서로 독립이라 상대 순서는 무관하다.** `program_access_grants`는 `sql/64`가, `performance_credit_ledger.grant_id` NOT NULL은 `sql/65`가 추가하며, 그 의존(`performance_credit_ledger`를 만드는 `54_performance_app.sql`이 `65_entitlement_grant_ledger`보다 먼저)은 번호 오름차순 실행이 이미 만족한다. 두 파일 모두 dev DB에는 이미 적용돼 있어 실질 피해는 없다. 파일 리네임(번호 정리)은 두 브랜치를 머지하는 쪽이 결정할 사안이라 여기서는 하지 않는다.

> ⚠️ **53~58번은 6개 전부가 이미 머지된 다른 브랜치의 같은 번호와 중복된다** — 54번 하나만의 문제가 아니다: `53_performance.sql`↔`53_mentor_apply_faq_admin.sql`·`53_pricing_susi_restore.sql`, `55_performance_guide_hardening.sql`↔`55_coupon_policy.sql`·`55_goal_management.sql`, `56_performance_topic_hardening.sql`↔`56_goal_jungsi_optional.sql`·`56_surrogate_uuid_keys.sql`, `57_performance_design_report.sql`↔`57_profiles_insert_hardening.sql`, `58_performance_submission.sql`↔`58_money_invariants.sql`(54번은 위 항목 참조). 원래 결정(Q4)은 "신규 슬라이스는 추가 시점의 최대 번호 다음 정수를 쓰고 한 파일로 합쳐 충돌 표면을 줄인다"였는데, 이 6개를 추가할 때는 이미 최대 번호가 71까지 가 있었는데도 그걸 확인하지 않고 직전 51/52번대의 번호 패턴만 보고 53부터 순차로 이어붙인 것으로 보이며, 단일 파일 병합 원안도 못 지켜 6개로 쪼개졌다. 지금 와서 72번대로 재번호하거나 6개를 한 파일로 합치는 정리는 **하지 않는다** — 여섯 파일 모두 dev DB에 이미 적용 완료라, 파일명이 배포 이력의 추적 키인 이 저장소 관례상(이 문서 전체가 그 전제로 쓰였다) 재구성하면 되레 dev dump 재생성·운영 반영 절차에서 혼란만 커진다. 번호 중복은 **알려진 상태로 그대로 유지**한다. 재발 방지: 앞으로 신규 파일을 추가할 때는 직전 파일의 번호 패턴을 눈대중으로 따라가지 말고, 반드시 `ls sql/*.sql`로 그 시점의 실제 최대 번호를 확인한 뒤 다음 정수를 쓸 것.

## 재실행 시 데이터 보존 원칙

- `10_pricing_orders.sql`의 products/coupons 시드는 `on conflict (slug) do nothing` — 이미 존재하는 slug는 재실행으로 절대 덮어쓰지 않는다(`sql/56_surrogate_uuid_keys.sql`의 id/slug 분리 이후 충돌 대상이 대체키 `id`에서 사람이 읽는 자연키 `slug`로 바뀌었다). `api/create-order.js`가 products를 결제 신뢰값으로 읽으므로, 단종 상품·종료 쿠폰의 가격/노출 상태가 파일 재실행으로 롤백되면 실제 청구 금액이 바뀐다.
- `20_landing_renewal.sql`의 `program_categories` 카피 갱신 UPDATE는 `public.schema_migrations` 마커 테이블로 **최초 1회만** 적용된다. 이후 재실행에서는 관리자가 어드민 화면에서 수정한 값을 그대로 보존한다.
- 신규 설치(빈 DB)에서는 위 가드와 무관하게 최초 시드가 정상적으로 들어간다 (모든 `insert ... where not exists` 블록은 계속 무조건 동작).
- `30_landing_admin_media.sql`의 멘토 22건 백필·히어로 배너 시드·배너 행 정리·레거시 멘토 비활성화도 각각 마커(`30_mentor_card_fields_backfill_v1` / `30_banners_hero_seed_v1` / `30_banners_hero_rows_cleanup_v1` / `30_legacy_mentor_rows_deactivate_v1`)로 **최초 1회만** 적용된다. 시드는 로컬경로(`/images/landing/…`)만 사용하며, Storage publicUrl은 환경별로 다르므로 업로드 스크립트가 환경별로 UPDATE한다.
- `43_learning_diagnosis_rename.sql`의 마커(`43_learning_diagnosis_rename_v1`)는 위 4종과 성격이 다르다 — 어떤 문장도 이 마커로 가드하지 않는다(rename은 테이블·인덱스가 `alter ... if exists`, 제약·트리거는 `pg_constraint`/`pg_trigger` 존재 확인 후에만 실행하는 do 블록으로 이미 멱등하고, 값 UPDATE는 `is distinct from` 가드가 아니라 구 값에만 매칭하는 where 절로 자연 멱등하다). 개명이 전부 끝난 상태에서만 기록되는 **적용 완료 감사 기록**이며, 마커를 지워도 재실행 결과는 동일하다.
- ⚠️ **`20_landing_renewal.sql`의 `program_categories` 신규 삽입 시드는 `where not exists (select 1 from program_categories pc where pc.name = v.name)` 가드라 name이 다르면 마커와 무관하게 재실행마다 다시 평가된다.** 이 시드의 `v.name`이 이번에 `학습진단`(신 이름)으로 갱신됐다 — 아직 `43_learning_diagnosis_rename.sql`을 적용하지 않아 값이 `무료진단`(구 이름)으로 남아 있는 **기존 DB**에 `20`을 재실행하면 `학습진단` 행이 새로 INSERT되어 중복이 생긴다(활성 행 7개 → 랜딩 서비스 카드가 `.slice(0, 6)`으로 하나 누락). 기존 DB에는 `20`을 재실행하지 말 것 — 이미 적용됐고 `20_landing_renewal_program_categories_backfill_v1` 마커로도 기록돼 있다. 부득이 재실행해야 한다면 `43`을 먼저 적용해 값을 신 이름으로 만든 뒤에 할 것. 신규 DB 구축(빈 테이블)은 순서대로 실행해도 안전하다 — `20`이 신 값으로 최초 삽입하고 `43`은 rename 대상이 없어 no-op이다.

## RLS admin 판정

`31_storage_policies.sql` 실행 이후 랜딩 테이블 write 판정은 `public.is_winning_admin()` (security definer, admin 4단계 role 체계) 기준으로 통일되어 있다. `university_acceptances`의 admin write 정책과 `storage.objects`(`banners` 버킷) insert/update/delete 정책이 모두 이 함수를 사용한다. `profiles` 테이블 자신의 RLS 정책이 `profiles`를 재참조하는 서브쿼리를 직접 쓰면 42P17 infinite recursion으로 관리자 쓰기가 전부 막히므로, 반드시 이 함수를 통해 판정한다. 새 admin write 정책을 추가할 때도 동일 패턴(`using (public.is_winning_admin())` / `with check (public.is_winning_admin())`)을 따를 것. (구 `public.is_admin()`은 20번 최초 도입 함수이며 31번에서 랜딩 테이블 판정은 `is_winning_admin()`으로 교체되었다.)

## 서버 전용 테이블 (RLS 정책 없음 + 권한 회수)

`40_auth_signup.sql`이 만드는 아래 세 테이블은 **RLS를 켜고 정책을 하나도 두지 않는다.** 정책 없는 RLS는 전면 거부이므로 `service_role`(= `api/` 서버리스 함수)만 접근할 수 있다.

| 테이블 | 담는 것 |
|---|---|
| `phone_verifications` | 휴대폰 인증코드 해시, 시도 횟수 |
| `link_code_lookups` | 연결코드 조회 이력 (한도 판정 + 감사) |
| `identity_verifications` | NICE 본인확인 결과 (CI/DI 포함) |

추가로 각 테이블에 `revoke all on table ... from anon, authenticated`를 건다. **Supabase는 `public` 스키마의 테이블·함수에 `anon`/`authenticated` 권한을 기본 부여하는데, `revoke ... from public`만으로는 지워지지 않는다.** PUBLIC 의사롤이 아니라 각 롤에 직접 걸린 권한이라 롤을 명시해 회수해야 한다. 함수도 같다 — `issue_student_link_code` 등 내부 전용 함수는 `revoke all on function ... from public, anon, authenticated`로 막고 필요한 롤에만 `grant execute` 한다.

> 조회가 안 된다는 이유로 이 테이블들에 select 정책을 추가하지 말 것. 인증코드는 발급한 서버만 알아야 하고, 조회 이력과 CI/DI는 본인조차 볼 이유가 없다.

## Supabase Auth 대시보드 설정 (코드에 없음)

가입 플로우는 **6자리 이메일 OTP**(`verifyOtp({ type: 'signup' })`)를 전제로 하는데, 그걸 성립시키는 설정이 코드가 아니라 대시보드에만 있다. 프로젝트를 새로 만들거나 설정이 되돌아가면 아래를 다시 맞춰야 한다.

| 위치 | 항목 | 값 |
|---|---|---|
| Authentication → Emails → Confirm signup | 템플릿 본문 | `{{ .Token }}` 사용, **`{{ .ConfirmationURL }}` 완전 제거** |
| Authentication → Emails → Magic Link | 템플릿 본문 | 위와 동일 (가입 이어가기 경로가 이 템플릿을 쓴다) |
| Authentication → Providers → Email | Email OTP Length | 6 |
| Authentication → Providers → Email | Email OTP Expiration | 600초 권장 (기본 3600초) |
| Authentication → Emails → SMTP | 커스텀 SMTP | 필수. 내장 메일러는 시간당 2통 수준으로 제한된다 |
| Authentication → Rate Limits | 시간당 발송 수 | 커스텀 SMTP 연결 후 상향 |

**`{{ .ConfirmationURL }}`을 남기면 안 되는 이유**: 매직링크를 누르면 Supabase가 이메일을 확인 처리하고 **세션까지 만들어 Site URL로 보내버린다.** 사용자가 가입 폼으로 돌아오지 않으므로 `complete_signup_profile`이 호출되지 않고, `auth.users`에는 있는데 `profiles`에는 없는 계정이 남는다. 그 이메일로는 재가입도 불가능해진다. 링크를 "추가로" 넣는 것도 같은 우회로를 열어두는 것이라 안 된다.

`40_auth_signup.sql` `[9] check_email_signup_state`가 그렇게 생긴 계정을 `resumable_*`로 판정해 이어서 가입시키지만, 애초에 만들지 않는 편이 낫다.

## 외부 연동 IP 화이트리스트

알리고(알림톡·SMS)와 NICE(본인확인)는 발신 IP를 화이트리스트로 검사한다. `api/` 함수의 외부 호출은 `api/_lib/outbound.js`를 거쳐 고정 IP 프록시(Fixie)로 나가야 하며, **프록시를 경유하지 않으면 벤더가 IP로 거절한다.** Supabase Edge Function에서 호출하면 이 프록시를 타지 않으므로 반드시 이 레포의 `api/`에서 나가야 한다.

Fixie의 아웃바운드 IP가 바뀌면 알림톡과 본인확인이 **동시에** 죽는다. 에러가 "IP 차단"으로만 나와 원인이 잘 안 보이므로, IP를 바꿀 일이 생기면 두 벤더 모두에 재등록할 것.
| 73 | `73_goal_daily_record_v2.sql` | 목표관리 일별 기록 수식 v2: `goal_daily_records.body_condition` CHECK를 원본 3지(`good`/`normal`/`bad`, v1) → 신시안 4지선다(`great`/`normal`/`tired`/`exhausted`, `src/data/goalStudyMock.js` mockConditionOptions)로 교체. 컬럼이 실측 0행이라 값 이관 UPDATE 불요, CHECK drop/add만. `body_condition`/`achievement`/`focus` 3개 컬럼 코멘트를 v2 기준(컨디션배수는 실제 사용, 성취도·집중도는 v2 UI 자체가 없어 `''` 고정)으로 갱신 | `55_goal_management.sql`의 `goal_daily_records` 필요 |

> 56~72 미문서화: 이 표는 55번 이후(다른 브랜치 병렬 작업 산물) 갱신되지 않았다 — 해당 파일들은 `sql/` 디렉터리에 실재하나 이 표엔 없다. 팀장 작업 지시서는 이번 파일을 72번으로 지정했으나 `goal-daily-record` 브랜치를 딴 시점(dev@9ef6686)에 이미 `72_learning_diagnosis_v2_survey_copy.sql`이 그 번호를 선점하고 있어 73으로 옮겼다(53번 회피 선례와 동일 사유 — 접두어 중복은 실행 사고로 이어진다).
