/**
 * 화면 전용 확장 영역(extras)·예시 리포트 표시가 쓰는 문자열·규칙 단일 테이블.
 *
 * 자체 결정(2026-08-12) 확정 — 원본 근거 없음. 변경 시 이 값만 교체. 관련 Q-07 · Q-22 · Q-16
 * (2026-08-11 초판은 신규 발주 대기 상태였다. 원저자 답을 기다리지 않고 우리가 정한 값을
 * 확정으로 승격했다 — 문구팀 답이 오면 그때 다시 교체한다.)
 *
 * 왜 diagnosisCopy.js 가 아니라 별도 파일인가:
 *   문구집(diagnosisCopy.js) 상단에는 문구 개수 검산(344 + 22)이 걸려 있어 한 키만 얹어도
 *   검산이 깨진다. 같은 이유로 REPORT_FALLBACK 이 이미 diagnosisReport.js 안에 격리돼 있는데,
 *   그쪽은 **엔진 폴백**이고 여기는 **화면 레이아웃 전용**이라 소비자가 다르다(컴포넌트만 읽는다).
 *
 * 왜 diagnosisReport.js 의 SELF_DECIDED 와 합치지 않는가:
 *   SELF_DECIDED 는 조립 계층이 ReportData 에 실어 내리는 값이고, 여기 값은 **엔진이 알 필요가
 *   없는 화면 구조 문자열**이다(섹션 제목·접기 라벨·몇 개를 펼칠지). 엔진에 두면 순수 조립 계층이
 *   화면 레이아웃 결정을 들고 있게 된다. 값이 두 벌로 갈라질 위험은 없다 — 키가 겹치지 않는다.
 *
 * 문구팀이 정식 문구를 발주하면 이 파일의 키를 문구집으로 이사시키고 파일을 삭제한다.
 */

export const SCREEN_EXTRAS = Object.freeze({
  copy: Object.freeze({
    /**
     * 자체 결정(2026-08-12) 확정. extras 최상단 h2 — 원본 근거 없음, 신규 발주 대기.
     * 아래 두 블록 소제목('영역별 상세 진단' · '맞춤 전략')을 그대로 이어 붙여, 학생이
     * h2 만 읽어도 이 구역에 무엇이 두 덩어리로 오는지 미리 알 수 있게 했다.
     */
    sectionTitle: '영역별 상세 진단과 맞춤 전략',

    /**
     * 자체 결정(2026-08-12) 확정. 원본 근거 없음, 신규 발주 대기. PDF 에 없는 이유를 학생이
     * 묻지 않게 하는 문장이다 — 화면에만 있는 내용을 아무 말 없이 두면 "인쇄가 잘렸다"로
     * 읽힌다(F-10 과 같은 계열의 오인 위험).
     */
    screenOnlyNote: '아래 내용은 화면에서만 제공되며 PDF에는 포함되지 않습니다.',

    /** 자체 결정(2026-08-12) 확정. 원본 근거 없음, 신규 발주 대기. 접기(disclosure)는 이 페이지 전체에서 이것 하나만 허용한다. */
    strategyMoreLabel: '나머지 영역의 맞춤 전략 보기',

    /**
     * 자체 결정(2026-08-12) 확정. 원본 근거 없음, 신규 템플릿(문구집 05_구간_공통의
     * '라벨 · 라벨' 조합 관례를 그대로 따랐다 — card_exec.sub '6개 영역 평균 · 목표 {target}점'
     * 과 같은 형식). {level} 은 URGENCY_LEVEL_LABEL(배점표 141행 원문)이고 {threshold} 는
     * URGENCY_AREA_THRESHOLD 다 — UI 에 리터럴 40 을 심으면 값이 두 벌이 된다.
     * urgency.score 는 **의도적으로 넣지 않는다**(내부 합성값이라 상한이 화면 어디에도 없어
     * 옆의 영역 점수 0~100 과 같은 척도로 오독된다).
     * lowAreaCount 가 0 이면 ' · ' 뒤 절은 통째로 뗀다(0개를 굳이 말하지 않는다).
     */
    urgencyLine: '이번 진단 긴급도 {level} · {threshold}점 미만 영역 {count}개',

    /** 자체 결정(2026-08-12) 확정. 원본 근거 없음, 신규 발주 대기. extras 마지막 블록(해석 한계 고지) 제목. */
    noticeTitle: '이 리포트를 읽을 때',

    areaDetailTitle: Object.freeze({
      /** 자체 결정(2026-08-12) 확정. 원본 근거 없음, 신규 발주 대기. 블록 A 제목. */
      section: '영역별 상세 진단',
      /**
       * **재사용**(창작 아님) — page1 은 요약 카드 제목 templateCopy('card_exec.title'),
       * page2 는 ReadinessOverview 의 정적 섹션 제목과 같은 문자열이다. 학생이 위에서 본
       * 두 묶음의 이름을 그대로 되풀이해야 6+6 소섹션이 어느 페이지와 짝인지 읽힌다.
       * page1 은 런타임에 문구집에서 조회하고 여기 값은 조회 실패 폴백으로만 쓴다.
       */
      page1: '학습 실행 역량',
      page2: '학교 생활 및 입시 준비도'
    }),

    /** **재사용** — 문구집 02 시트의 구분명('맞춤 전략') 그대로. '먼저 할 일'은 TYPE_COPY.todos 가 쓸 자리라 피했다. */
    strategyTitle: '맞춤 전략'
  }),

  rules: Object.freeze({
    /**
     * 자체 결정. 48개(12영역×4)를 상시 펼치면 읽을 수 없고 학생이 실제로 착수할 수 있는 개수도
     * 소수다. 하위 3영역만 펼치고 나머지 9영역은 접는다. 기존 관례와 정렬이 같다
     * (traits = PAGE1 하위 3 · improvements = 12영역 하위 4 → 하위 3 은 그 부분집합).
     */
    strategyFocusCount: 3,

    /**
     * 킬 스위치. 블록 A(영역별 상세 12행)는 F-21(문구-점수 상충)의 노출 면적을 12배로 키운다 —
     * 문구 재검수에서 문제가 나오면 이 한 줄을 false 로 바꿔 블록 전체를 끈다.
     */
    showAreaDetails: true,

    /**
     * 자체 결정(2026-08-12) 확정 — 원본 근거 없음. 관련 F-10 · G-3 NIT 2
     *
     * 입결 비교표에 비교 대상(cut50/cut70) 행이 하나도 없을 때(G-1b `hasRows=false`) 어떻게
     * 보여줄지 결정하는 롤백 레버다. F-10 이 '빈 표 숨김'으로 닫혔지만(AdmissionSection 이
     * `hasRows` 로 박스 자체를 뺀다) 그 레버가 코드에 하드코딩돼 있었다 — 여기로 뽑아 뒀다.
     *   'HIDE'       (기본값·현행) — 박스를 통째로 렌더하지 않는다.
     *   'NOTICE_ROW' — 박스는 그리되 표 대신 안내 행 1개를 넣는다. 새 문구를 만들지 않는다 —
     *                  diagnosisReport.js REPORT_FALLBACK.BAND_VALUE_NODATA('자료 없음')를 재사용한다.
     */
    admissionEmptyTableMode: 'HIDE' // 'HIDE' | 'NOTICE_ROW'
  })
});

/**
 * 예시(픽스처) 리포트 표시 문구. 자체 결정(2026-08-12) 확정 — BANNER·워터마크 2건은
 * 원본 근거 없음, 신규 발주 대기. 변경 시 이 값만 교체. 관련 F-18
 *
 * F-18 은 오인 위험 2위였다: 가상 학생의 완결 리포트가 아무 표시 없이 본인 결과처럼 렌더된다.
 * 화면 배너만으로는 부족하다 — 인쇄물은 그대로 남의 리포트로 유통되므로 인쇄에는 시트마다
 * 대각선 워터마크를 넣는다(문서 흐름 밖 ::after 라 A4 2장 높이에 영향 0).
 * CTA 라벨은 창작이 아니라 LearningDiagnosisLanding 의 기존 버튼 문구 인용이다.
 */
export const SAMPLE_REPORT_COPY = Object.freeze({
  BANNER: '이 화면은 리포트 구성을 보여 드리는 예시입니다. 학습진단을 완료하시면 학생 본인의 결과로 바뀝니다.',
  CTA: '학습진단 시작하기',
  CTA_HREF: '/learning-diagnosis/survey/1',
  /**
   * G-3(NIT 5, 2026-08-12) — 종전엔 CSS `content: '예시'` 리터럴이라 JS 문구 검사·격리 스캔
   * 어느 쪽에도 안 잡혔다(정의처가 report-print.css 하나뿐이었다). 정의처를 여기로 옮기고
   * CSS 는 `content: attr(data-watermark)` 로 이 값을 **주입**만 받는다(ReportSheetA4.jsx 가
   * `data-watermark` 속성으로 내려보낸다) — 이제 findBannedPhrases 같은 JS 스캔이 이 문자열도
   * 정상적으로 훑는다. 자체 결정(2026-08-12) 확정, 원본 근거 없음. 변경 시 이 값만 교체.
   */
  WATERMARK: '예시'
});

export default SCREEN_EXTRAS;
